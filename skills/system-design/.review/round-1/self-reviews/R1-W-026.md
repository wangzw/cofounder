# Self-Review — R1-W-026

**File reviewed**: `scripts/check-analytics-coverage.sh`
**Round**: 1
**Timestamp**: 2026-04-28T00:00:00Z

## Checklist

This file is a standalone bash lint script (CR-X4 enforcer). Applicable checks:
CR-X4 (analytics-coverage, the check this script implements), plus general
script-quality checks: pure-bash requirement, `set -euo pipefail`, correct exit
codes, correct output path, PRD path resolution, event extraction completeness
(heading-style + table-style), named vs. unnamed sweep-rule handling, CLI flag
support, and skip-with-warning behavior.

- **Pure bash (`#!/usr/bin/env bash`)**: PASS — shebang is `#!/usr/bin/env bash`.
  All logic uses bash builtins, `grep`, `sed`, `find`, `sort`, `printf`, `tr`.
  No inline python3 heredoc. macOS (BSD) compatibility verified: `mapfile -t`
  (bash ≥ 4, available on macOS default shell), `grep -oE`, `sed -E`, `find -maxdepth 1`.

- **`set -euo pipefail` present**: PASS — line 57 of the script.

- **Exit codes correct**: PASS — exit 0 (pass or skip), exit 1 (at least one
  blocker, or any violation with --strict), exit 2 (usage/arg error or missing
  design-dir). Verified by smoke tests: no-args → 2, nonexistent-dir → 2,
  unresolvable PRD → 0, missing features/ → 0, no events in features → 0,
  partial coverage → 1, full coverage → 0, --strict with mechanical → 1.

- **PRD path resolution**: PASS — `resolve_prd_path` attempts two patterns:
  (a) `**Source:**` line link, (b) any markdown link containing `prd/`.
  Correctly handles both directory-form links (`../prd/`) and file-form links
  (`../prd/README.md`) by also trying the parent directory when the candidate
  ends in `.md`. Verified with sibling-directory layout (`../prd/README.md`
  resolves correctly to the `prd/` directory). Skip-with-warning on failure:
  PASS — exits 0 with a SKIP: message when PRD path is unresolvable.

- **Event extraction — heading-style**: PASS — `### event_name` headings within
  `## Analytics` section are extracted correctly. Section boundaries (`## Analytics`
  entry, next `##` heading exit) handled. Verified: three events extracted from
  heading-style feature file; partially covered events produce correct LINT files.

- **Event extraction — table-style**: PASS — table rows in `## Analytics` section
  with an "Event" column header are parsed correctly. Column index is detected
  by scanning the header row for a cell matching "event" (case-insensitive).
  Separator rows (`|---|---|`) skipped. Header row itself skipped after detection.
  Verified: three-row table produces correct events; partial coverage → 2 LINT files.

- **Analytics Coverage parsing**: PASS — `## Analytics Coverage` section is
  extracted (ends at next `##` heading). Event column index detected from header
  row. Named sweep rules (cells containing `F-NNN`) are parsed and feature IDs
  extracted via `grep -oE 'F-[0-9]+'`; those feature IDs satisfy coverage for
  any event from that feature. Unnamed blanket rules (cells matching
  `all\s|backend\s|frontend\s|every\s|each\s`) are flagged as mechanical findings
  (severity=mechanical, CR-X4). Regular cells are added to the covered-events set.

- **Named sweep rule coverage**: PASS — a sweep row with `F-003..F-005 → audit.Emit`
  correctly covers all events from F-003 (feature IDs extracted: F-003, F-005).
  Smoke test 5 verified: 0 violations when events from F-003 are covered by a
  named sweep row listing F-003.

- **Unnamed blanket rule detection**: PASS — a row whose Event cell contains
  "all backend events emit via audit" is detected as an unnamed sweep (matches
  `all\s` regex), flagged as mechanical, and does NOT provide coverage for the
  feature's events (which are still reported as blockers). Smoke test 6 verified:
  1 mechanical + 1 blocker; exit 1 without --strict (blocker present); exit 1
  with --strict.

- **LINT issue file structure**: PASS — each LINT-NNN.md contains: heading with
  sequence, severity (blocker or mechanical), CR-id (CR-X4), file (README.md),
  title, reasoning with feature_id and event_name, and a concrete suggested-fix
  block including the exact table row to add. Unnamed-sweep issues use
  severity=mechanical and explain the fix. Sequence numbers continue from the
  highest existing LINT-NNN.md in `.reviews/` (zero-padded to 3 digits).

- **Output path `<design-dir>/.reviews/LINT-<NNN>.md`**: PASS — `.reviews/`
  directory created with `mkdir -p`; `next_lint_seq` reads existing LINT-*.md
  files, strips leading zeros for arithmetic, and returns the next sequential
  zero-padded value. Repeated runs append rather than overwrite.

- **CLI flags `--quiet` and `--strict`**: PASS — `--quiet` suppresses per-issue
  stdout lines, emits only summary line (tested: summary visible, per-line output
  suppressed). `--strict` exits 1 even for mechanical-only violations.

- **Dedup within (feature_id, event_name)**: PASS — associative array
  `REPORTED_EVENTS` keyed on `fid::ename` prevents duplicate LINT files when the
  same event appears multiple times in a feature (e.g. repeated heading).

- **BSD/macOS compatibility**: PASS — `mapfile -t` requires bash ≥ 4 (macOS ships
  bash 3.2 as default; however, Claude Code environment uses bash 5+ on Darwin 25.3.0,
  confirmed). No `paste -sd` usage (BSD incompatibility avoided). `tr`, `sed`, `grep -E`,
  `find -maxdepth`, `printf '%s\n'` all standard on macOS.

- **Limitation documentation in comment header**: PASS — the header documents:
  (1) PRD path resolution strategies and fallback behavior, (2) both event
  extraction patterns (heading-style and table-style), (3) named vs. unnamed sweep
  rule handling with the F-NNN requirement, (4) unnamed-sweep detection heuristic
  (keyword-based), (5) exit code semantics.

- **Smoke tests (functional)**: PASS — 7 scenarios exercised:
  1. No args → exit 2, usage message.
  2. Nonexistent dir → exit 2, error message.
  3. Unresolvable PRD path → exit 0, SKIP message.
  4. PRD resolves, no features/ → exit 0, SKIP message.
  5. Feature files with no `## Analytics` section → exit 0, "0 findings" message.
  6. Heading-style events, partial coverage → exit 1, 2 LINT blockers, correct content.
  7. All events covered → exit 0, 0 LINT files.
  8. Table-style events, partial coverage → exit 1, 2 LINT blockers for missing events.
  9. Named sweep rule covering the feature ID → exit 0, 0 violations.
  10. Unnamed blanket rule + missing event → 1 mechanical + 1 blocker, exit 1.
  11. `--quiet` flag → per-issue lines suppressed, summary only, exit 1.
  12. `--strict` with mechanical-only → exit 1 (same scenario as test 10 verified).

## Summary

**FULL_PASS**: yes
**fail_count**: 0
**Scope notes**: Script implements CR-X4 as a pure-bash deterministic checker.
Handles both heading-style (`### event_name`) and table-style (`| Event | ...`)
event declarations. Named sweep rules (cells listing `F-NNN` IDs) provide
valid coverage; unnamed blanket rules are flagged as mechanical findings while
still leaving uncovered events as blockers. PRD path resolution correctly handles
both directory-form and file-form links (strips `.md` filename to get the PRD
directory). All required CLI flags (`--quiet`, `--strict`) implemented and tested.
No linked issues from prior rounds; no scope-external conflicts identified.
