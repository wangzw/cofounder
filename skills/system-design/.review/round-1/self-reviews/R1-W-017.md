# Self-Review — R1-W-017

**File reviewed**: `revise/index.md`
**Round**: 1
**Timestamp**: 2026-04-28T00:00:00Z

## Checklist

`revise/index.md` is a mode-routing orchestrator entry file — not a sub-agent prompt and not
SKILL.md. Applicable CRs: CR-L02 (self-contained-file), CR-L01 (orchestrator-pure-dispatch).
CR-S08 checked for absence (not a sub-agent prompt → Snippet D footer correctly absent).
CR-S01/S02/S09 apply only to SKILL.md — not applicable here.

- **CR-L02 self-contained-file**: PASS — The file documents the full 8-step orchestration
  sequence inline with no cross-file lookups required. All system-design-specific overrides are
  present in-file: output path convention (`<design-dir>/.reviews/` user-facing directory distinct
  from `.review/` internal harness), REVIEW-*.md and LINT-*.md inventory scope (exclude
  `*.applied.md`), lint pre-pass gate (Step 3 — re-run `scripts/run-checkers.sh` before any LLM
  dispatch; blocker LINT findings must be resolved first), per-issue reviser fan-out scoped to
  ONE artifact leaf per reviser (Step 4), lint post-pass verification with loop cap (Step 5),
  summarizer consolidation of REVISIONS.md (Step 6), `git mv` rename to `.applied.md` (Step 7),
  and user-facing print template (Step 8). A consuming orchestrator needs only this file to
  execute the --revise pipeline.

- **CR-S08 ipc-footer-present**: PASS (N/A) — The header explicitly states "not a sub-agent
  prompt and does not carry the Snippet D fingerprint." Snippet D IPC footer is correctly absent.

- **CR-L01 orchestrator-pure-dispatch**: PASS — The orchestrator is instructed to collect ACK
  fields only (linked_issues) from revisers; it never reads artifact leaf content. All leaf
  reading is delegated to per-issue-reviser-subagent. Summarizer receives only reviser ACK fields
  and placeholder REVISIONS.md lines. The orchestrator's only direct write targets are `state.yml`
  and `dispatch-log.jsonl` (per the Core Contract in SKILL.md). No artifact summarization,
  content analysis, or convergence verdict computation by the orchestrator.

- **Script-then-LLM ordering preserved**: PASS — Step 3 (lint pre-pass via
  `scripts/run-checkers.sh`) executes before Step 4 (LLM reviser fan-out). This matches the
  legacy `revise-mode.md` Step 7.0 gate and the authoring guidance's "Lint pre-pass gate" step.
  Blocker LINT findings gate LLM dispatch; non-blocker findings may be batched with LLM revisers.

- **`.applied.md` rename in --revise (not --review)**: PASS — Step 7 explicitly performs
  `git mv REVIEW-007.md REVIEW-007.applied.md` (and equivalent for LINT files) in --revise only.
  The file explicitly states "`.applied.md` rename happens HERE (in `--revise`), NOT in
  `--review`" — matching the authoring guidance hard rule. The sister `review/index.md` states
  the same boundary from the other side. No conflation risk.

- **Scope-containment hard rule present**: PASS — Step 4 and the Hard Rules section both state
  each reviser opens ONLY the files named in its issue. "Multi-issue revises are still ONE
  REVISIONS.md entry per --revise invocation" is captured via the summarizer consolidation step
  (Step 6), matching the authoring guidance.

- **No auto-chain into --review**: PASS — Hard Rules section explicitly states "--revise MUST NOT
  auto-chain into --review. Exit after Step 8." Matches the canonical skill-forge revise/index.md
  boundary rule.

- **Output path convention alignment with review/index.md and SKILL.md**: PASS — Output path
  table lists `.reviews/` (user-facing, capital S) for all reviser outputs and renamed files.
  No conflation with `.review/` (internal harness). Consistent with `review/index.md` Output Path
  Reference table and SKILL.md's Output Structure section.

- **Post-pass lint loop with cap**: PASS — Step 5 specifies loop-back to Step 4 for newly
  surfaced findings with cap at `config.yml convergence.max_iterations`. Matches the authoring
  guidance "Any newly surfaced findings = write new LINT-*.md and loop."

- **User print template matches authoring guidance**: PASS — Step 8 emits
  "<N> issues applied, <M> modules touched. Run `/cofounder:system-design --review` to verify."
  exactly matching the authoring guidance Step 7 template.

- **Length within target**: PASS — File is approximately 175 lines, within the 120–200 line
  target specified in the authoring guidance.

## Summary

**FULL_PASS**: yes
**fail_count**: 0
**Scope notes**: All applicable CRs passed. The file correctly implements the system-design
--revise orchestration by (1) preserving the script-then-LLM ordering with a lint pre-pass gate
that blocks LLM dispatch on blocker findings, (2) adopting the `<design-dir>/.reviews/` output
convention with correct LINT/REVIEW file inventory and `.applied.md` rename lifecycle, (3)
enforcing per-reviser scope containment (one issue → one artifact leaf, no widening),
(4) placing `.applied.md` rename exclusively in --revise (not --review), (5) consolidating all
closed issues into a single REVISIONS.md entry per invocation via the summarizer, (6) providing
a lint post-pass verification loop with a convergence cap, and (7) printing the exact user-facing
summary the authoring guidance specifies. No scope-external conflicts identified.
