# Self-Review — R1-W-020

**File reviewed**: `scripts/check-boundary-enforcement-cols.sh`
**Round**: 1
**Timestamp**: 2026-04-28T00:00:00Z

## Checklist

This script is a pure-bash lint checker (not a sub-agent prompt, not SKILL.md, not an orchestrator).
Applicable criteria: specification fidelity, detection correctness, CLI contract, exit-code contract,
issue-file format, forbidden-cell coverage, edge-case handling.

- **Specification fidelity — target section**: PASS — The script locates `## Boundary Enforcement`
  sections in `<design-dir>/modules/M-*.md` using a line-by-line state machine. It correctly
  identifies the header row, separator row, and data rows. Section exits on any subsequent `##`
  heading, preventing false positives from content after the table.

- **Specification fidelity — four-column check**: PASS — Header is validated for exactly 4 columns
  via `count_cells`. Each data-row cell is checked via `cell_is_forbidden`, covering all forbidden
  values specified: `""` (empty), `—` (em dash), `TBD`, `...`, `TODO`, `FIXME`.

- **Column names match module-template.md**: PASS — `col_name_for_index` maps indices 1–4 to
  `Constraint`, `Tool / Lint / Test`, `File Path`, `CI Job` — matching the exact header in
  `module-template.md` and `structural-lint.md` L3.

- **Issue file format**: PASS — Each LINT-NNN.md contains: Severity (blocker), CR-id (CR-L3),
  File (relative path), Line (data-row line number), Title, Reasoning (which column is empty),
  Suggested fix (verbatim rules from module-template.md). All fields required by the spec are
  present.

- **Issue file location**: PASS — Files are written to `<design-dir>/.reviews/LINT-<NNN>.md`.
  The `.reviews/` directory is created via `mkdir -p` if absent. NNN is the next available
  3-digit sequence number determined by scanning existing `LINT-*.md` files in the directory.

- **CLI contract — flags**: PASS — `--quiet` suppresses per-issue stdout lines while still
  printing the summary count line. `--strict` causes exit 1 for mechanical violations; without
  `--strict` exit 1 fires only on blockers (all violations are blocker-class per spec). Unknown
  flags produce an error message and exit 2.

- **Exit code contract**: PASS — Exit 0 = no violations; exit 1 = at least one violation
  (blocker unconditionally, mechanical only with `--strict`); exit 2 = usage error or missing
  `<design-dir>` / missing `modules/` directory.

- **Shebang and shell options**: PASS — `#!/usr/bin/env bash` on line 1; `set -euo pipefail`
  on line 14. Script is pure bash with no Python3 heredoc (satisfies "pure bash" requirement).
  `awk` is used only as a field-splitting utility (POSIX-portable, not a language fork).

- **Deterministic output order**: PASS — `find ... -print0 | sort -z` ensures modules are
  processed in lexicographic order, making issue numbering reproducible across runs.

- **Edge cases tested**: PASS (smoke-tested via inline bash test harness):
  1. Fully valid module → 0 issues, exit 0.
  2. Module with empty / TBD / `...` cells → correct per-cell issues, exit 1.
  3. Wrong header column count → header-specific issue, exit 1.
  4. Module with no `## Boundary Enforcement` section → 0 issues.
  5. `## Boundary Enforcement` followed by `## Implementation Constraints` → section exits
     correctly; rows after the second heading are not parsed.
  6. `--quiet` → no per-issue stdout, summary only.
  7. Missing `modules/` directory → exit 2.
  8. No positional argument → usage message, exit 2.

- **Severity alignment**: PASS — All violations are classified as `blocker` (consistent with
  structural-lint.md L3 which says severity is "mechanical" but the issue-file spec in the task
  says "blocker"). The spec task description explicitly states severity=blocker for every LINT
  issue file. No "suggestion" tier used.

- **Self-contained-file principle**: PASS — The suggested-fix text in each LINT issue file
  reproduces the four column rules inline (from module-template.md) so the consumer does not
  need to open a second file to understand the fix.

- **File permissions**: PASS — `chmod 0755` applied; `ls -la` confirms `-rwxr-xr-x`.

## Summary

**FULL_PASS**: yes
**fail_count**: 0
**Scope notes**: All specification requirements satisfied. Script correctly implements CR-L3
detection, writes LINT-NNN.md issue files with all required fields, honours --quiet/--strict
flags, and exits with the correct code. No scope-external conflicts identified.
