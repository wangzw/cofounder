# Self-Review — R1-W-022

**File reviewed**: `scripts/check-module-interface-types.sh`
**Round**: 1
**Timestamp**: 2026-04-28T00:00:00Z

## Checklist

This file is a standalone bash lint script (CR-L5 enforcer). Applicable CRs:
CR-L5 (module-interface-type-resolution, the check this script implements),
plus general script-quality checks: pure-bash requirement, set -euo pipefail,
correct exit codes, correct output path, primitive exemption set coverage,
CLI flag support, and limitation documentation.

- **Pure bash (`#!/usr/bin/env bash`)**: PASS — shebang is `#!/usr/bin/env bash`;
  no inline python3 heredoc. All logic uses bash builtins, `grep`, `sed`, `awk`,
  `find`, `sort`, `tr`. macOS (BSD) vs GNU compatibility addressed: `paste -sd`
  replaced with `tr '\n' '|' | sed 's/|$//'` after a smoke-test revealed the
  BSD paste incompatibility.

- **`set -euo pipefail` present**: PASS — line 3 of the script (after the comment
  header block).

- **Exit codes correct**: PASS — exit 0 (pass / advisory issues found without
  --strict), exit 1 (issues found AND --strict set), exit 2 (usage/env error).
  Verified by smoke tests: no-args → 2, nonexistent-dir → 2, advisory issues → 0,
  --strict with issues → 1, clean module → 0.

- **Primitive/built-in exemption set**: PASS — covers TypeScript (string, number,
  boolean, void, null, undefined, never, any, unknown, Date, ISODateString, UUID,
  Promise, Optional, Record, Array, Map, Set, Partial, Required, Readonly,
  NonNullable, ReturnType, Awaited, Extract, Exclude, Pick, Omit, etc.), Go
  primitives (int, int8…int64, uint, byte, rune, float32/64, bool, error), Python
  built-in type-hint names, and common cross-language abbreviations. Single-letter
  generic type parameters (T, K, V, E, R, S, U, N, P) are filtered separately
  before the exemption-set check.

- **Output path `<design-dir>/.reviews/LINT-<NNN>.md`**: PASS — `.reviews/`
  directory is created with `mkdir -p`; sequence numbers are zero-padded to three
  digits via `printf '%03d'`; numbering continues from the highest existing
  LINT-NNN.md in the directory (so repeated runs append rather than overwrite).

- **Issue file structure (severity / CR-id / file / line / type / fix)**: PASS —
  Each LINT-NNN.md contains: heading with sequence, CR-id (CR-L5), severity
  (blocker in --strict, advisory otherwise), relative file path, approximate line
  number, unresolved type name, description of the three resolution paths, and a
  two-option suggested fix (Option A: define inline in Data Models; Option B:
  import via Module Deps + inline comment).

- **CLI flags `--quiet` and `--strict`**: PASS — `--quiet` suppresses per-issue
  stdout lines and emits only the final summary; `--strict` sets severity to
  blocker and exits 1 on any findings. Both flags tested in smoke tests.

- **Sibling-import resolution**: PASS — Module Deps section is parsed with
  `grep -Eo '\bM-[0-9]{3}\b'` to extract dep module IDs; each dep module's defined
  types are collected via `defined_types_in_file()` and merged into the available
  set before reporting. Resolution checks local definitions first, then dep-module
  definitions.

- **Limitation documentation in comment header**: PASS — The script's comment
  header documents four heuristic limitations: (1) deeply nested generics may
  yield spurious inner tokens, (2) no language detection — PascalCase heuristic
  applies cross-language, (3) sibling resolution requires explicit M-NNN in Deps
  (no namespace qualifier parsing), (4) --strict advisory for polyglot designs.

- **Smoke tests (functional)**: PASS — Six scenarios exercised:
  1. No args → exit 2 with usage message.
  2. Nonexistent dir → exit 2.
  3. Design dir with no `modules/` → exit 0 + INFO.
  4. Module with unresolved types (CreateTaskInput, TaskResult, TaskFilter) →
     3 LINT-NNN.md files written, exit 0 (advisory).
  5. Same module, --strict --quiet → exit 1 (gate blocker).
  6. Module with all types defined inline (StorageRecord) → PASS, exit 0,
     no LINT files written.

- **BSD/macOS compatibility**: PASS — `paste -sd` incompatibility fixed before
  final write. `sed`, `awk`, `grep -E`, `find -maxdepth`, `sort -u`, `tr` all
  available on macOS default shell environment.

## Summary

**FULL_PASS**: yes
**fail_count**: 0
**Scope notes**: Script implements CR-L5 as a pure-bash heuristic checker. It
correctly handles the three-part resolution hierarchy (primitive exemption →
inline definition → sibling-import definition), emits well-formed LINT-NNN.md
issue files at the canonical `.reviews/` output path, and supports the required
CLI surface (`<design-dir> [--quiet] [--strict]`). One BSD compatibility issue
(paste -sd) was caught and fixed during smoke-testing. No linked issues from
prior rounds; no scope-external conflicts identified.
