# Self-Review — R1-W-015

**File reviewed**: `generate/new-version.md`
**Round**: 1
**Timestamp**: 2026-04-28T10:45:00Z

## Checklist

See `generate/in-generate-review.md` for CR applicability table. `generate/new-version.md` is a
mode-routing orchestrator entry file — not a sub-agent prompt and not SKILL.md. Applicable CRs
for this file type (any artifact leaf): CR-L02. Also checking CR-S08 (not applicable — this file
is not a sub-agent prompt, confirmed by "is not a sub-agent prompt" note in the Notes section).
CR-S01/S02/S09 apply only to SKILL.md. CR-L01 checked for completeness since this is an
orchestrator-level file.

- CR-L02 self-contained-file: PASS — The file documents the full 13-step dispatch sequence
  inline. All system-design-specific overrides (MODULE-granularity plan shape, two-phase quality
  gate, REVISIONS.md vs CHANGELOG distinction, post-delivery cascade notification) are described
  in-file without requiring the reader to cross-reference revise-mode.md or the canonical
  skill-forge new-version.md. A consuming orchestrator needs only this file to execute the
  NewVersion pipeline.
- CR-S08 ipc-footer-present: PASS (N/A) — This file explicitly states "not a sub-agent prompt"
  and "does not carry the Snippet D fingerprint" in the Notes section; the Snippet D IPC footer
  is correctly absent.
- CR-L01 orchestrator-pure-dispatch: PASS — The orchestrator is instructed to NOT read any
  artifact leaf other than `plan.md` (Step 7) and `verdict.yml` (Step 13). The file explicitly
  prohibits orchestrator reads of design content, relying on ACK fields only. Delete operations
  are performed by the orchestrator directly (git rm), not via sub-agent write, consistent with
  §19.1.

## Summary

**FULL_PASS**: yes
**fail_count**: 0
**Scope notes**: All applicable CRs passed. The file correctly inherits the canonical skill-forge
new-version.md dispatch contract (Steps 1–7 and 12–13 are structurally identical to the
canonical) while adding system-design-specific overrides: MODULE-granularity plan shape with
classification rules for delete/modify/add/keep; two-phase quality gate (Step 10 structural lint
gate before Step 11 LLM review); adversarial reviewer dispatched in parallel with cross-reviewer;
summarizer writes REVISIONS.md entry (not CHANGELOG section); post-delivery cascade notification
referencing autoforge. Round numbering example spans 3 deliveries to illustrate cross-delivery
monotonic numbering with multi-round deliveries. No scope-external conflicts identified.
