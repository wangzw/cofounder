# Self-Review — R1-W-029
## Artifact
`skills/system-design/scripts/check-single-source-of-truth.sh`

## Evaluation against CR-X7 requirements

| # | Criterion | Result | Notes |
|---|-----------|--------|-------|
| 1 | Pure bash, no third-party deps | PASS | Uses only `bash`, `awk`, `find`, `grep`, `mktemp`, `mkdir`, `rm`, `cat`, `printf`, `basename` — all standard POSIX/macOS toolchain. No `jq`, `python`, `node`, etc. |
| 2 | `#!/usr/bin/env bash` shebang | PASS | Line 1: `#!/usr/bin/env bash` |
| 3 | `set -euo pipefail` | PASS | Line 69: `set -euo pipefail` |
| 4 | CLI: `<design-dir> [--quiet] [--strict]` | PASS | Argument parser (lines 76–100) accepts positional `<design-dir>` plus `--quiet` / `--strict` flags in any order; rejects unknown flags with exit 2. |
| 5 | Exit code 0 (no violations) | PASS | Lines 563–566: prints `OK 0 findings` and exits 0 when `VIOLATION_COUNT == 0`. |
| 6 | Exit code 1 (violation found AND blocker or --strict) | PASS | Lines 570–572: exits 1 when `HAS_BLOCKER == 1` OR `STRICT == 1`; exits 0 otherwise (mechanical-only findings without --strict). |
| 7 | Exit code 2 (usage error / dir not found) | PASS | Lines 96–105: missing `<design-dir>` → exit 2; directory not found → exit 2; unknown flag → exit 2. |
| 8 | Conservative detection — case 1 (data models) implemented | PASS | AWK extractor (lines 246–401) targets `## Data Models` sections in TypeScript-style fenced code blocks for `interface`, `type`, `class`, `enum`, `struct` declarations. |
| 9 | Case 2 (endpoint signatures) noted as TODO | PASS | Lines 50–56: explicit TODO comment for endpoint-signature drift; deferred with rationale ("too many formatting variants"). |
| 10 | Case 3 (boundary-enforcement rule copies) noted as TODO | PASS | Lines 57–60: explicit TODO comment deferring to X6/L3. |
| 11 | Severity = `blocker` for non-byte-equal duplicates | PASS | Lines 510–525: `emit_issue "blocker"` called when `primary_def != dup_def`. |
| 12 | Severity = `mechanical` for byte-equal copies without excerpt marker | PASS | Lines 539–554: `emit_issue "mechanical"` called when content is equal but `<!-- excerpt-from: <slug> -->` marker is absent. |
| 13 | Output path: `<design-dir>/.reviews/LINT-NNN.md` | PASS | `REVIEWS_DIR="$DESIGN_DIR/.reviews"` (line 111); issue files written to `$REVIEWS_DIR/LINT-${seq}.md` (line 154). |
| 14 | chmod 0755 | PASS | `ls -la` confirms `-rwxr-xr-x` (owner rwx, group r-x, other r-x). |

## Additional observations (non-blocking)

- **Sequence numbering safety**: `next_lint_seq` scans existing `LINT-*.md` files to avoid overwriting prior issues; this is correct and conservative.
- **Tmp dir cleanup**: `rm -rf "$TMP_TYPES_DIR"` is called after the group phase; no tmp leak on the happy path. Note: a `trap` on ERR/EXIT would protect cleanup on early `set -e` exits — acceptable omission given conservative scope.
- **Empty scan_files guard**: `"${scan_files[@]+"${scan_files[@]}"}"`  pattern (line 404) correctly handles the empty-array case under `set -u`.
- **Primary selection heuristic**: primary is determined by sort order from `find | sort`, which gives alphabetical order per subdirectory. This is deterministic and documented in the comment (lines 494–499).
- **Brace-counting limitation**: The AWK extractor counts `{` / `}` characters naively; it does not ignore braces inside string literals or comments. This is an accepted false-positive/false-negative risk for exotic type definitions — acceptable under the conservative-detection mandate.

## Verdict

**FULL_PASS** — all 14 CR-X7 criteria pass. No blockers.
