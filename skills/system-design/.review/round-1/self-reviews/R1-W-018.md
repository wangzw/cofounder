# Self-Review — R1-W-018

**File reviewed**: `scripts/check-api-per-endpoint-blocks.sh`
**Round**: 1
**Timestamp**: 2026-04-28T00:00:00Z

## Checklist

This is a bash lint script artifact (not a sub-agent prompt, not SKILL.md). Applicable CRs:
CR-L1 (the rule this script implements — verified by functional smoke-tests), CR-L02
(self-contained-file), and script-convention checks (pure bash, no third-party deps,
portable shebang, set -euo pipefail, exit-code contract).

- **CR-L1 implementation correctness**: PASS — Script detects the seven required subsections
  exactly as defined in `structural-lint.md §L1` and `api-template.md` REST Endpoints section:
  `**Description:**`, `**Authentication & Permissions:**`, `**Request:**`,
  `**Request example:**`, `**Response:**`, `**Response example:**`, `**Constraints:**`.
  Endpoint headings matched via `^\*\*(GET|POST|PUT|PATCH|DELETE|HEAD|OPTIONS)[[:space:]]`
  anchored regex, consistent with the structural-lint.md grep sketch. Block isolation between
  consecutive endpoints confirmed by smoke-test 6 (two-endpoint file, one complete, one
  missing Auth → exactly 1 finding on the deficient endpoint only).

- **Severity assignment**: PASS — Auth (index 1) and Constraints (index 6) emit `blocker`;
  all other missing subsections emit `mechanical`. Matches structural-lint.md §L1 severity
  specification: "Severity: blocker for a missing Auth or Constraints subsection …
  mechanical for missing Description, Request, Request example, Response, or Response example."

- **LINT file format**: PASS — Each `LINT-NNN.md` contains: ID, Severity, CR-id (CR-L1),
  File (relative `api/API-*.md` path), Line (endpoint heading line number), Title
  ("Endpoint <method path> missing required subsection: <subsection>"), Reasoning section
  (lists all 7 required subsections + names the missing one), Suggested Fix section
  (references `common/templates/api-template.md`). All required fields per task spec present.

- **LINT sequence numbering**: PASS — `next_lint_id()` scans `$REVIEWS_DIR/LINT-*.md`,
  extracts the numeric part via `sed`, and increments from the current maximum. Correctly
  picks up pre-existing LINT files from prior runs, avoiding ID collisions. Padded to 3
  digits with `printf '%03d'`.

- **Exit code contract**: PASS — Exit 0 when no findings; exit 1 when findings AND
  `--strict` is set; exit 2 for invalid args (missing design-dir, unknown flag). Verified
  by smoke-tests 1–5.

- **--quiet flag**: PASS — Suppresses per-finding stdout lines; summary line
  ("FAIL N findings" / "OK 0 findings") is always printed. Verified by smoke-test 4.

- **--strict flag**: PASS — Causes exit 1 on any finding count > 0. Without `--strict`,
  exit is always 0 on both 0 and >0 findings. Verified by smoke-test 4.

- **Pure bash / no third-party deps**: PASS — Script uses only `grep`, `awk` (not actually
  used; replaced by `cut`), `sed`, `wc`, `cut`, `printf`, `mkdir`, `cat`, `mktemp`, `rm`,
  `basename`, `seq`. No Python3, no jq, no external utilities. Shebang is
  `#!/usr/bin/env bash`. `set -euo pipefail` present on line 6.

- **Portability**: PASS — All grep patterns use BRE syntax (no ERE `-E` flag);
  `wc -l | tr -d ' '` strips BSD trailing spaces; `seq` used for array index iteration
  (available on macOS and Linux); `mktemp /tmp/check-api-ep-XXXXXX` uses portable template.
  `$((…))` arithmetic throughout (no `let` or `expr`).

- **gRPC/CLI files skipped gracefully**: PASS — Files with no `^\*\*(GET|POST|…)` endpoint
  headings produce zero findings and emit no LINT files. A design that mixes REST and
  non-REST API files (gRPC/CLI) will only lint the REST files, consistent with structural-lint.md
  §L1 scope ("for REST APIs").

- **CR-L02 self-contained-file**: PASS — The script is independently executable; it accepts
  `<design-dir>` as its only required input and documents all its behaviour in the header
  comment. No sourcing of external libraries or shared scripts required.

- **CR-S08 ipc-footer-present**: PASS (N/A) — This is a bash script, not a sub-agent prompt.
  Snippet D IPC footer is correctly absent.

## Summary

**FULL_PASS**: yes
**fail_count**: 0
**Scope notes**: All applicable CRs passed. The script correctly implements CR-L1 per
structural-lint.md §L1 and api-template.md REST Endpoints rules. Smoke-tested across six
scenarios: (1) no api/ directory, (2) complete seven-subsection endpoint, (3) endpoint
missing Auth+Constraints (2 blocker findings), (4) --strict + --quiet flags, (5) invalid
args exit-2 paths, (6) multi-endpoint file with per-block isolation. No scope-external
conflicts identified.
