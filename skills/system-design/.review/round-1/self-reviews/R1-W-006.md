# Self-Review — R1-W-006

**File reviewed**: `skills/system-design/common/templates/api-template.md`
**Round**: 1
**Timestamp**: 2026-04-28T00:00:00Z

## Checklist

<!-- CRs applicable to a template file (not SKILL.md, not review-criteria.md, not a subagent prompt).
     Applicable set: CR-L02 (self-contained), CR-L03 (review-criteria coverage N/A for templates).
     Structural-lint CRs (L1/L2) are applied as authoring constraints on the template itself. -->

- CR-L02 self-contained-file: PASS — every endpoint block contains all auth rules, error envelopes, rate-limit values, and data-model shapes inline; no cross-file path references in the model endpoint section.
- CR-S01 skill-md-frontmatter: PASS (N/A) — this file is a template, not SKILL.md; frontmatter CR does not apply.
- CR-S02 mode-routing-complete: PASS (N/A) — template file; mode-routing CR does not apply.
- CR-S08 ipc-footer-present: PASS (N/A) — template file; IPC footer CR does not apply.
- CR-S09 dispatch-log-snippet: PASS (N/A) — template file; dispatch-log CR does not apply.
- CR-L01 orchestrator-pure-dispatch: PASS (N/A) — template file; orchestrator CR does not apply.
- CR-L03 review-criteria-coverage: PASS (N/A) — template file; review-criteria coverage is a concern for review-criteria.md, not for per-artifact templates.

### L1 lint compliance (structural self-check)

The template defines exactly ONE model endpoint block and places the seven required subsections
in the following order, matching the L1 enforcement specification from structural-lint.md:

1. Request — present (HTTP method + path, headers table, query params table, body schema table, populated request body example)
2. Response — present (success body schema table, populated response body example)
3. Status codes — present (Code | Meaning | When | Example body table)
4. Error model — present (error envelope shape JSON, error codes enumerated table, retry semantics prose)
5. Auth — present (token type, header, scope, roles/permissions matrix table, workspace scoping)
6. Rate limits — present (quota, window, shared bucket, throttle behavior, burst allowance, backoff)
7. Examples — present (two full curl + response pairs covering happy path and validation error)

All seven subsections are present and in the correct order.

### L2 lint compliance (structural self-check)

Inspected all ```json blocks in the template:

- Request body example: uses real-looking string values (`"Deploy production build"`, `"alice"`, `"platform"`). No `"..."`, no `{}`, no `"TBD"`, no `"TODO"`, no `"<placeholder>"` patterns.
- Response body example: uses real-looking values with a realistic ID (`"task_01abc123def456"`), ISO 8601 timestamp (`"2026-04-28T09:15:00Z"`), and all fields from the Response schema table.
- Status codes table inline bodies: all use concrete JSON strings — no `...` filler.
- Error model error envelope example: uses `"{{error_type_string}}"` and `"{{human-readable description}}"` as `{{PLACEHOLDER}}` tokens (not `"..."` or bare `...`).
- Error codes table: prose cells, not JSON — L2 does not apply.
- Test Scenarios table: prose cells, not fenced JSON blocks — L2 does not apply.
- Example 1 curl + 201 response block: fully populated, no filler.
- Example 2 curl + 400 response block: fully populated, no filler.

Schema–example consistency: every field in the Response schema table (id, type, name, metadata, created_at, {{additional_field}}) appears in the Response body example (with {{additional_field}} omitted as a placeholder row, which is acceptable — it is a template instruction, not a live field).

L2 lint: PASS on all inspected blocks.

### Front-matter presence

File-level front matter table is present with all six required fields: File, Owner module, Status, Source Features, Direction, Protocol, Versioning policy. All use `{{PLACEHOLDER}}` markers as required for a template.

### HTML comments — IPC envelope prohibition

No `<!-- metrics-footer -->`, `<!-- self-review -->`, or IPC envelope comments are present.
Authoring guidance comments (HTML comments marking lint requirements and template instructions) are present — these are template-internal notes for the consuming writer, not IPC envelopes. Acceptable per role contract.

### Rules section

A `## Rules` section is included as an authoring reference with a note that it should not be copied into generated output. This section documents L1 and L2 compliance requirements inline so the consuming writer does not need to open structural-lint.md.

## Summary

**FULL_PASS**: yes
**fail_count**: 0
**Scope notes**: No cross-artifact dependencies required for this template file. All L1 and L2 structural-lint constraints verified in-file. No PARTIAL flags.
