# Self-Review — R1-W-030

**File reviewed**: `scripts/check-readme-references.sh`
**Round**: 1
**Timestamp**: 2026-04-28T00:00:00Z

## Checklist

This file is a standalone bash lint script (CR-X8 enforcer). Applicable CRs:
CR-X8 (readme-references, the check this script implements),
plus general script-quality checks: pure-bash requirement, set -euo pipefail,
correct exit codes, correct output path, CLI flag support, and link-parsing
correctness per the X8 spec in `structural-lint.md`.

- **Pure bash (`#!/usr/bin/env bash`)**: PASS — shebang is `#!/usr/bin/env bash`;
  no python3 or other interpreter invocations. All logic uses bash builtins,
  `grep`, `sed`, `awk`, `printf`, `find`, `wc`. `awk` is used for path
  normalisation (`.`/`..` component resolution). All utilities are available in
  the macOS default shell environment (BSD-compatible).

- **`set -euo pipefail` present**: PASS — appears at line 3 of the script body
  (after the comment header block).

- **Exit codes correct**: PASS — exit 0 (no violations), exit 1 (violations
  present AND (blocker found OR --strict set)), exit 2 (usage/env error). Verified
  by smoke tests 1–12: no-args → 2, nonexistent-dir → 2, no-README.md → 2,
  blocker violations → 1, mechanical-only without --strict → 0,
  mechanical-only with --strict → 1, clean design → 0.

- **Link parsing — external URL skip**: PASS — `http://`, `https://`, and
  `mailto:` prefixes are detected via `case` statement and skipped before any
  resolution attempt. Verified in smoke test 8: `[GitHub](https://github.com/foo)`
  and `[email](mailto:a@b.com)` produce zero violations.

- **Link parsing — anchor-only skip**: PASS — targets starting with `#` are
  skipped. Verified in smoke test 8: `[overview](#overview)` produces no
  violation.

- **Fragment/query suffix stripping**: PASS — `?...` suffix stripped via
  `"${raw_target%%\?*}"` and `#...` suffix stripped via `"${stripped_target%%\#*}"`.
  A fragment-suffixed link to an existing file produces no violation (smoke test 9:
  `modules/M-001-auth.md#some-section` resolves correctly). A fragment-suffixed
  link to a missing file produces a blocker (smoke test 8).

- **Path resolution — relative to README.md's dir**: PASS — script resolves all
  relative targets by prepending `DESIGN_DIR` (README.md's directory). Absolute
  paths (`/...`) are used as-is. `./` and `../` prefixes are handled naturally via
  awk-based path normalisation that removes `.` and `..` components. Verified:
  clean design with `modules/M-001-auth.md` and `api/API-001-users.md` resolves
  correctly (smoke test 3, 0 violations).

- **Severity classification**: PASS — three tiers:
  - Cross-doc PRD refs (`../../../prd/...` pattern via `case`): **mechanical**.
    Verified: smoke test 5 emits LINT-001 with `Severity: mechanical` and exit 0.
  - All other broken relative paths (including module and API refs): **blocker**.
    Verified: smoke tests 4 and 8 emit LINT files with `Severity: blocker` and
    exit 1. Note: the spec states module refs (`modules/M-NNN-{slug}.md`) and API
    refs (`api/API-NNN-{slug}.md`) MUST be blockers — they fall into the default
    blocker branch correctly.

- **Output path `<design-dir>/.reviews/LINT-<NNN>.md`**: PASS — `.reviews/`
  directory created with `mkdir -p`; sequence numbers zero-padded to 3 digits via
  `printf '%03d'`; numbering continues from highest existing LINT-NNN.md so
  repeated runs append rather than overwrite (smoke test 12: second run on a dir
  with LINT-001/002 produces LINT-003+).

- **LINT file structure (severity / CR-id / file / line / target / reasoning / fix)**:
  PASS — each LINT-NNN.md contains heading with sequence number, Severity,
  CR-id (CR-X8), File (README.md), Line (source line number), Target (the raw
  link target), Reasoning paragraph quoting the CR-X8 rule from `structural-lint.md`,
  and a concrete Suggested Fix ("fix the path, remove the broken link, or create
  the referenced file"). Verified: LINT-001.md content from smoke test 4 contains
  all required fields.

- **CLI flags `--quiet` and `--strict`**: PASS — `--quiet` suppresses per-issue
  stdout lines and summary line (matching the pattern used by existing scripts
  `check-endpoint-literal-vs-api.sh` and `check-feature-module-traceability.sh`).
  `--strict` forces exit 1 even when all findings are mechanical. Verified:
  smoke test 6 (--strict with mechanical-only PRD ref → exit 1),
  smoke test 7 (--quiet on blocker findings → no stdout, exit 1).

- **Unknown flag handling**: PASS — unknown flags print ERROR + usage to stderr
  and exit 2 (smoke test 10).

- **README.md absent**: PASS — script exits 2 with a clear error message when
  `README.md` does not exist in the design dir (smoke test 11).

- **LINT sequence appending (no overwrites)**: PASS — `next_lint_seq` scans
  existing `LINT-NNN.md` files, finds the max N, and returns N+1. Repeated runs
  append new files rather than overwriting (smoke test 12: 2 violations × 3 runs
  → LINT-001 through LINT-006, all intact).

- **BSD/macOS compatibility**: PASS — `grep -oE`, `sed`, `awk` (BSD awk),
  `printf`, `wc -l`, `tr -d ' '` all confirmed available on macOS default
  environment. No GNU-only flags. `case`-based prefix matching avoids extended
  bash regex (`=~`) for the URL skip — safer across shell versions.

- **Smoke tests (functional)**: PASS — 12 scenarios exercised:
  1. No args → exit 2 with usage message.
  2. Nonexistent dir → exit 2 with error message.
  3. Clean design (all links resolve: module, API, external URL, anchor) →
     0 violations, exit 0.
  4. Two broken links (module ref + misc ref) → 2 blocker LINT files, exit 1.
  5. Cross-doc PRD ref (../../../prd/...) missing → 1 mechanical LINT file,
     exit 0 (no blocker, no --strict).
  6. --strict with mechanical-only finding → exit 1.
  7. --quiet with blocker findings → no stdout output, exit 1.
  8. External URLs + anchor-only + fragment/query-suffixed broken links →
     skips correctly; 2 blocker LINT files for fragment and query suffix cases.
  9. Fragment-suffixed link to existing file → 0 violations, exit 0.
  10. Unknown flag → exit 2.
  11. No README.md in design dir → exit 2.
  12. Sequence appending: second run on dir with LINT-001/002 → LINT-003+ created,
      no overwrites.

## Summary

**FULL_PASS**: yes
**fail_count**: 0
**Scope notes**: Script implements CR-X8 as a pure-bash README link resolver.
It correctly skips external URLs, anchor-only links, and query/fragment suffixes;
resolves relative paths (including `..` traversal via awk-based normalisation)
from README.md's directory; classifies PRD cross-doc refs as mechanical and all
other broken links as blocker; emits LINT-NNN.md files at the canonical
`.reviews/` path with all required fields; and supports `--quiet`/`--strict`
flags. LINT sequence is append-safe (no overwrites). All 12 smoke tests pass;
BSD/macOS compatibility confirmed. No linked issues from prior rounds; no
scope-external conflicts identified.
