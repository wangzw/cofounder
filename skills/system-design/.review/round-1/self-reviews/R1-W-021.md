# Self-Review — R1-W-021

**File reviewed**: `scripts/check-api-surface-cols.sh`
**Round**: 1
**Timestamp**: 2026-04-28T00:00:00Z

## Checklist

This is a bash lint script (not a skill file). Applicable CRs: functional correctness,
output contract adherence, CLI contract adherence, and issue-file format per the task spec.
Skill-structure CRs (CR-S01, CR-S08, etc.) do not apply to standalone bash scripts.

### Functional correctness

- **Detection — header column count**: PASS — `count_pipe_cols()` counts pipe-delimited
  cells after stripping leading/trailing `|`. A header with ≠ 7 columns triggers a blocker
  issue. Verified in Test 3 (6-col header → 2 violations: header + data row).

- **Detection — data row column count**: PASS — Each non-header, non-separator pipe row is
  counted independently. Wrong column count produces a separate issue file. Verified in
  Test 3.

- **Detection — empty/forbidden cell values**: PASS — `is_empty_cell()` matches all six
  forbidden values: `""`, `"—"`, `"TBD"`, `"..."`, `"TODO"`, `"FIXME"`. Per-cell iteration
  reads each cell via `split_pipe_row()` + `while read`. Verified in Test 2 (TBD → 1
  violation) and Test 7 (blank cell → 1 violation).

- **Separator row skipped**: PASS — `is_separator_row()` uses regex stored in a variable
  (`sep_re`) to avoid bash `[[ =~ ]]` `|`-in-literal parse error. Separator rows are skipped
  before column counting. Verified: no false positives on `|---|---|` rows in any test.

- **Section boundary detection**: PASS — `in_api_surface` is set on `## API Surface` (or
  variant) and cleared on any subsequent `## ` heading. Multi-section files handled correctly.
  Verified in Test 1 (API Surface followed by `## Other Section`).

- **No API Surface section → clean pass**: PASS — Module file with no `## API Surface`
  heading produces 0 violations. Verified in Test 6.

- **No modules/ dir → clean pass**: PASS — Script detects absent `modules/` and exits 0
  with informational message. Verified in Test 5.

### CLI contract

- **`<design-dir>` positional argument**: PASS — Required first argument; error + exit 2 if
  absent or not a directory.

- **`--quiet` flag**: PASS — Suppresses per-violation stdout and summary lines; issue files
  still written. Verified in Test 4b (empty stdout, 1 issue file, exit 1).

- **`--strict` flag**: PASS — Accepted without error (reserved; currently same behavior as
  default). Unknown flags produce error + exit 2.

- **Exit codes**: PASS — 0 on no violations, 1 on any violation, 2 on usage/I/O error.
  Verified across all tests.

### Issue file format

- **Required fields present**: PASS — Every issue file contains: `**Severity**: blocker`,
  `**CR-id**: CR-L4`, `**File**: <rel-path>`, `**Line**: <N>`, Finding section, Suggested Fix
  section. Column issues additionally carry `**Column**: N — <name>`. Verified in Test 7
  by reading LINT-001.md content.

- **Sequence numbering**: PASS — `next_lint_seq()` scans existing LINT-NNN.md files in
  `.reviews/`, strips leading zeros, and increments from the maximum. New directories start
  at LINT-001. Verified: multiple violations produce LINT-001, LINT-002, … in order.

- **`.reviews/` directory created if absent**: PASS — `mkdir -p "$REVIEWS_DIR"` runs before
  the scan loop.

- **Relative file path in issue body**: PASS — `rel_file="${module_file#"$DESIGN_DIR/"}"` 
  strips the design-dir prefix; issue files reference `modules/M-NNN-slug.md` not absolute
  paths.

### Code quality

- **`set -euo pipefail`**: PASS — Present at top of script.

- **`#!/usr/bin/env bash`**: PASS — Correct shebang.

- **Bash regex variable indirection**: PASS — Regex pattern stored in `sep_re` variable to
  avoid the `[[ =~ ]]` `|`-in-literal bug. Syntax-checked with `bash -n`.

- **No stray bare-word commands**: PASS — Two stray bare-words (`col_count` and `seq`) from
  draft editing were removed before final verification. Confirmed with `bash -n` and full
  test run.

- **chmod 0755**: PASS — Applied via `chmod 0755` after write.

### Smoke tests run

| Test | Scenario | Expected | Result |
|------|----------|----------|--------|
| T1 | Clean module, all 7 cols filled | exit 0, 0 issues | PASS |
| T2 | `TBD` in Error Codes | exit 1, 1 issue | PASS |
| T3 | 6-col header | exit 1, 2 issues | PASS |
| T4b | `--quiet` flag | exit 1, empty stdout, 1 issue file | PASS |
| T5 | No modules/ dir | exit 0, 0 issues | PASS |
| T6 | No API Surface section | exit 0, 0 issues | PASS |
| T7 | Blank cell + issue file content | exit 1, all fields present | PASS |

## Summary

**FULL_PASS**: yes
**fail_count**: 0
**Scope notes**: All detection paths verified (header count, data-row count, empty/forbidden
cells). CLI flags --quiet and --strict behave per spec. Issue file format includes all
required fields (severity, CR-id, file, line, column, finding, suggested fix). Bash syntax
clean; no stray commands; regex alternation bug avoided via variable indirection.
