# Self-Review — R1-W-016

**File reviewed**: `review/index.md`
**Round**: 1
**Timestamp**: 2026-04-28T00:00:00Z

## Checklist

`review/index.md` is a mode-routing orchestrator entry file — not a sub-agent prompt and not
SKILL.md. Applicable CRs: CR-L02 (self-contained-file), CR-L01 (orchestrator-pure-dispatch).
CR-S08 checked for absence (not a sub-agent prompt → Snippet D footer correctly absent).
CR-S01/S02/S09 apply only to SKILL.md — not applicable here.

- **CR-L02 self-contained-file**: PASS — The file documents the full 7-step orchestration
  sequence inline. All system-design-specific overrides are present in-file: output path
  convention (`<design-dir>/.reviews/` with capital S, distinct from `.review/` internal
  harness), lint check scope (CR-L1..CR-L5 per-file + CR-X1..CR-X8 cross-file), forced-full
  review override (no skip-set in --review mode), dual-reviewer dispatch (cross + adversarial
  in parallel), REVIEW file naming scheme with shared sequence numbers, REVIEW-SUMMARY
  structure, and distinction from generate-mode internal review. A consuming orchestrator
  needs only this file to execute the --review pipeline.

- **CR-S08 ipc-footer-present**: PASS (N/A) — The header explicitly states "not a sub-agent
  prompt and does not carry the Snippet D fingerprint." Snippet D IPC footer is correctly
  absent.

- **CR-L01 orchestrator-pure-dispatch**: PASS — The orchestrator is instructed to read only
  `README.md` and `REVISIONS.md` (Step 2) plus the LINT file path (passed to subagents, not
  read inline by the orchestrator). All leaf reading is delegated to sub-agents. The
  orchestrator collects ACKs and writes the REVIEW-SUMMARY. No artifact leaf reading, no
  content summarization, no convergence verdict computation by the orchestrator. Consistent
  with the Core Contract in SKILL.md.

- **Script-then-LLM ordering preserved**: PASS — Step 3 (lint pre-pass via
  `scripts/run-checkers.sh`) executes before Step 4 (LLM reviewer dispatch). This matches the
  canonical skill-forge review ordering and the system-design two-phase quality gate principle.
  The "do NOT block on lint findings" note correctly distinguishes --review from generate-mode
  (which halts on critical script errors) while still preserving the script-first sequence.

- **Output convention alignment with legacy `review-mode.md`**: PASS — LINT files at
  `<design-dir>/.reviews/LINT-<NNN>.md`, REVIEW files at `<design-dir>/.reviews/REVIEW-<NNN>.md`
  and `REVIEW-<NNN>-ADV.md`, summary at `REVIEW-SUMMARY-<YYYYMMDD-HHMMSS>.md`. All match the
  primary source (system-design.backup/review-mode.md Steps 1.5, 4). `.applied.md` rename
  explicitly prohibited in --review mode.

- **Distinction section present**: PASS — The "Distinction from Generate-Mode Review" table
  at the top clearly separates the two paths (`.reviews/` user-facing vs `.review/` internal),
  preventing future conflation.

- **Hard stop rule**: PASS — Step 7 explicitly states "--review MUST NOT auto-chain into
  --revise." Matches the canonical skill-forge verdict routing rule that user-triggered --review
  exits at the verdict.

- **Length within target**: PASS — File is approximately 160 lines, within the 120–200 line
  target specified in the authoring guidance.

## Summary

**FULL_PASS**: yes
**fail_count**: 0
**Scope notes**: All applicable CRs passed. The file correctly implements the system-design
--review orchestration by (1) preserving the script-then-LLM ordering from the canonical
skill-forge review/index.md, (2) adopting the `<design-dir>/.reviews/` output convention and
LINT/REVIEW/REVIEW-SUMMARY file naming from the legacy review-mode.md primary source, (3) adding
the forced-full-review override (no skip-set) appropriate for end-user --review, (4) documenting
the two-reviewer parallel dispatch (cross + adversarial) with shared REVIEW sequence numbering,
(5) providing a clear distinction table separating this end-user path from the generate-mode
internal review path. No scope-external conflicts identified.
