---
id: R2-015
status: new
severity: critical
criterion_id: CR-META-skeleton-protected
file: "scripts/lib/aggregate.py"
round: 2
source: orchestrator
linked_issues: ["R2-001"]
---

# CR-META-skeleton-protected

R2-001 (CR-S12) reports a SHA256 mismatch on `scripts/lib/aggregate.py`, which is listed in `common/skeleton/shared-scripts-manifest.yml` as a skeleton-protected file. Per `revise/index.md` Step 2, the orchestrator MUST NOT dispatch a reviser to modify skeleton-owned paths.

## Reasoning

`scripts/lib/aggregate.py` is verbatim-copied from skill-forge's `attachments/metrics-aggregate/` and pinned by sha256 in the skill-forge skeleton manifest (`scripts/lib/aggregate.py` entry). The current copy has drifted from the upstream pin (`expected ef316d46…, got 659f6abe…`).

This is a skeleton defect/drift, not an artifact-leaf revision target. Per `revise/index.md` Notes line 92: "Skeleton-protected files are never revised by the reviser. If a checker fires on a skeleton file, this indicates a skeleton defect — surface it as a HITL issue, not a reviser task."

## Suggested Fix (HITL)

The operator MUST resolve this manually by one of:

1. Restore `scripts/lib/aggregate.py` from skill-forge upstream (`/Users/wangzw/workspace/cofounder/skills/skill-forge/scripts/lib/aggregate.py`) and re-pin via `scripts/seal-skeleton.sh` if the upstream copy has legitimately advanced.
2. If the local drift is intentional (e.g., a genuine bugfix not yet propagated upstream), update the sha256 pin in `common/shared-scripts-manifest.yml` to match the new content AND propagate the fix back to skill-forge.

Either way, the orchestrator cannot resolve this in-loop and the issue is escalated for HITL review.

## Linked Issue

- R2-001 (CR-S12: SHA256 mismatch — skeleton-protected file modified)
