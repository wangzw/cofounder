SKILL_NAME: "prd-analysis"
SKILL_VERSION: "0.1.0"
SKILL_DESCRIPTION: "Use when the user needs to create a Product Requirements Document, perform product requirements analysis, convert sparse product ideas or brainstorming notes into structured self-contained PRDs optimized for AI coding agents, or evolve an existing PRD for a new iteration. Triggers: /prd-analysis, 'write a PRD', 'product requirements', 'requirements analysis'."
ARTIFACT_ROOT: "docs/raw/prd/"

clarification_at: "2026-04-25T00:37:30Z"
trace_id: "R0-C-001"
hitl_mode: "delegated-proceed"

normalized_requirements:
  R-001:
    value: "prd-analysis"
    status: confirmed
    evidence: "User prompt: 'I want a skill named `prd-analysis`'. Slug is canonical kebab-case; namespace is the cofounder plugin (per project CLAUDE.md, skills live in skills/<name>/ and are invoked as /cofounder:<name>)."
  R-002:
    value: "document"
    status: confirmed
    evidence: "User prompt: 'self-contained multi-file Product Requirements Documents (PRDs) optimized for AI coding agents'. PRD = markdown pyramid (README.md index + journeys/*.md + features/*.md + architecture.md + architecture/*.md). Reference implementation prd-analysis.backup confirms markdown-only output. Maps directly to skeleton/document/."
  R-003:
    value: |
      Multi-file markdown pyramid rooted at docs/raw/prd/YYYY-MM-DD-{product-slug}/. Pyramid shape:
        - README.md                       — pyramid index: product overview, journey index table, feature index table, roadmap, cross-journey patterns
        - journeys/J-NNN-{slug}.md        — one self-contained journey per leaf (touchpoints, pain points, mapped features)
        - features/F-NNN-{slug}.md        — one self-contained feature per leaf (data models, conventions, journey context COPIED inline; ≤300 lines)
        - architecture.md                 — concise (~50–80 line) architecture index
        - architecture/{topic}.md         — topic leaves (tech-stack, data-model, coding-conventions, security, etc.; standalone)
        - prototypes/                     — optional seed code + screenshots (not generated by this skill in delivery-1)
        - REVISIONS.md                    — append-only, created on first --revise
      ID conventions: F-NNN, J-NNN, M-NNN (zero-padded, sequential, stable across iterations).
      Self-contained file principle: every leaf must be readable in isolation — copy referenced context inline, never cross-reference.
      Leaf size ≤300 lines (per domain-glossary `leaf` definition).
    status: confirmed
    evidence: "Output Structure block in input.md (lines 109–148); project CLAUDE.md 'Output Conventions' and 'Self-Contained File Principle' sections; reference prd-analysis.backup directory shape."
  R-004:
    value: |
      Conversational sparse-input mode is the primary modality (matches FromScratch generative-skill pattern):
        /cofounder:prd-analysis "<one-line product idea>"
      Optional flags inherited from skeleton/document mode-routing:
        --target <path>       (NewVersion / evolve)
        --review --target <path>
        --revise --target <path>
        --diagnose [--round N | --delivery N | --since <iso>]
      File-reference inputs (e.g. notes.md) supported via @-expansion in the conversational input itself
      (handled by prepare-input.sh + dir-mode=selective for directory references). No standalone positional
      file argument in delivery-1 — users paste path-with-@ inside the description string.
    status: confirmed
    evidence: "Skeleton SKILL.md mode-routing table; user's prompt itself uses @skills/prd-analysis.backup as a directory reference, exercising the @-expansion pathway; delivery-1 keeps surface area minimal."
  R-005:
    value: |
      Script-type structural review criteria (run by scripts/run-checkers.sh, deterministic, ~5 CRs at delivery-1):
        CR-S-001  artifact-pyramid-shape    — README.md present at root; journeys/, features/, architecture/ subdirs present; ≥1 leaf in features/
        CR-S-002  leaf-size-cap             — every *.md leaf under journeys/, features/, architecture/ ≤300 lines
        CR-S-003  id-format-and-uniqueness  — feature filenames match F-\d{3}-[a-z0-9-]+\.md (likewise J-, M-); IDs unique within their dir
        CR-S-004  cross-link-integrity      — every relative markdown link from README.md / architecture.md resolves to an existing file
        CR-S-005  self-contained-discipline — feature leaves contain a "Data Models", "Conventions", and "Journey Context" section header (presence check, not semantic)
      All five are checker_type: script per domain-glossary; live in scripts/checkers/*.sh and aggregate via scripts/lib/aggregate.py.
    status: confirmed
    evidence: "prd-analysis.backup/review-checklist.md (~50-dimension list, of which the structural slice maps to these 5); skill-forge skeleton/document scripts/run-checkers.sh + scripts/lib/aggregate.py shape; project CLAUDE.md 'Self-Contained File Principle' and 'ID Conventions'."
  R-006:
    value: |
      Semantic LLM-type review criteria (evaluated by cross-reviewer + adversarial-reviewer subagents, ~6 CRs at delivery-1):
        CR-L-001  scope-discipline           — PRD captures product-level decisions only; no implementation/system-design leakage
        CR-L-002  journey-feature-coverage   — every journey touchpoint is addressed by ≥1 feature; every feature traces to ≥1 touchpoint
        CR-L-003  cross-journey-pattern-resolution — each declared cross-journey pattern in README is addressed by ≥1 feature
        CR-L-004  self-contained-readability — a coding agent reading ONE feature leaf can implement it without opening any other file (data models / conventions / journey context fully inlined, semantically complete)
        CR-L-005  mvp-discipline             — features marked must-have are genuinely MVP; nice-to-haves are deferred to roadmap, not silently scoped in
        CR-L-006  ambiguity-elimination      — no requirement is interpretable two ways; acceptance criteria are testable
      All six are checker_type: llm; surfaced as issue files under .review/round-N/issues/.
    status: confirmed
    evidence: "prd-analysis.backup/review-checklist.md dimensions; project CLAUDE.md 'Key Principles' (scope, MVP, no ambiguity, copy-don't-reference); guide §7 generative-skill pattern (cross-reviewer + adversarial-reviewer)."
  R-007:
    value: "not-applicable for delivery-1 (FromScratch). New-version semantics will be exercised when a user later runs /cofounder:skill-forge --target skills/prd-analysis '<change>' to evolve this skill, or when end-users run /cofounder:prd-analysis --target <prd-path> to evolve a PRD they own. Both flows already covered by the inherited skeleton/document NewVersion mode-routing — no skill-specific overrides required."
    status: not-applicable
    evidence: "input-meta.yml + trigger-flags.yml indicate FromScratch (no --target flag). Round 0 only resolves R-001..R-006 substantively; R-007 is structurally present but inherits skeleton defaults."

domain_terms_aligned:
  - term: "PRD"
    user_clarification: "Product Requirements Documents (PRDs) optimized for AI coding agents"
    resolved_to: "artifact (per domain-glossary): a multi-file pyramid rooted at docs/raw/prd/YYYY-MM-DD-{product-slug}/, with README.md index + leaves under journeys/, features/, architecture/. Not a single file."
  - term: "skill"
    user_clarification: "I want a skill named prd-analysis that converts sparse product ideas into ... PRDs"
    resolved_to: "generative skill (per domain-glossary): produces artifacts from sparse user input; uses 8-role pattern (orchestrator + 7 subagents) with iterative review/revise rounds. NOT a workflow skill."
  - term: "self-contained"
    user_clarification: "self-contained multi-file"
    resolved_to: "self-contained file (per project CLAUDE.md glossary): every leaf independently readable; referenced context (data models, conventions, journey context) COPIED inline rather than cross-referenced. Constrains R-005 CR-S-005 and R-006 CR-L-004."
  - term: "feature / journey / module"
    user_clarification: "(implicit from reference implementation prd-analysis.backup)"
    resolved_to: "leaf (per domain-glossary): a single bounded markdown file at the bottom of the artifact pyramid; ≤300 lines; one feature, one journey, or one module per leaf. IDs F-NNN / J-NNN / M-NNN per project CLAUDE.md."
  - term: "AI coding agent"
    user_clarification: "optimized for AI coding agents"
    resolved_to: "downstream consumer that reads ONE feature leaf and implements it. This consumer-shape directly motivates CR-L-004 self-contained-readability and the ≤300-line leaf cap."

variant_replay:
  variant: "document"
  skeleton_path: "skills/skill-forge/common/skeleton/document/"
  one_paragraph_summary: |
    Your prd-analysis skill will produce markdown-based PRD artifact directories
    (the document variant — pyramid of README.md + journeys/*.md + features/*.md
    + architecture.md + architecture/*.md, rooted at docs/raw/prd/YYYY-MM-DD-{slug}/).
    The skill itself follows the baseline 8-role generative-skill shape: orchestrator
    dispatches a domain-consultant (clarifies sparse input), a planner (decomposes
    into leaf-writing tasks), parallel writer subagents (one per feature / journey /
    architecture topic), then review/revise rounds driven by ~5 script-type checkers
    and ~6 LLM-type checkers, with a summarizer and judge closing each round. There
    are no variant-specific extensions beyond the baseline document skeleton — code
    execution, schema validation, and hybrid behaviors are all NOT applicable.
  user_alignment_note: "HITL delegated; treated as /proceed. R-001..R-006 derived from input.md + reference implementation; R-007 not-applicable for delivery-1."
