# Flat placeholder keys — REQUIRED top-level mapping consumed by scripts/scaffold.sh.
# scaffold.sh's parse_yaml_simple reads only top-level flat `KEY: "value"` lines;
# these four keys must be present BEFORE any nested block.
SKILL_NAME: "prd-analysis"
SKILL_VERSION: "0.1.0"
SKILL_DESCRIPTION: "Use when the user needs to create a Product Requirements Document, perform product requirements analysis, convert brainstorming notes into structured specs, or prepare requirements for AI coding agents. Triggers: /prd-analysis, 'write a PRD', 'product requirements', 'requirements analysis'."
ARTIFACT_ROOT: "docs/raw/prd/"

clarification_at: "2026-04-24T13:45:00Z"
trace_id: "R0-C-001"
mode: "FromScratch"
sparse_input: true
hitl_delegated: true
proceed_policy: "auto-proceed; mark deferred when truly ambiguous, confirmed when clearly inferrable from @skills/prd-analysis.backup reference implementation"

normalized_requirements:
  R-001:
    # Target skill name and namespace
    value: "prd-analysis"
    status: confirmed
    rationale: "Explicitly named in user prompt: 'I want a skill named `prd-analysis`'. Namespace is cofounder (plugin skill)."
  R-002:
    # Artifact type: document | code | schema | hybrid
    value: "document"
    status: confirmed
    rationale: "Backup reference implementation is entirely markdown-based (SKILL.md + *-template.md + *-mode.md topic files). PRDs are multi-file markdown directory trees (README.md index + journeys/ + architecture/ + features/). No code generation, no schemas, no binary outputs — pure document variant per common/skeleton/document/."
  R-003:
    # Artifact structure (file count, naming, index shape)
    value: "Multi-file pyramid under docs/raw/prd/YYYY-MM-DD-{product-slug}/. Root: README.md (index + product overview + journey/feature indices + roadmap). Subdirectories: journeys/J-001-{slug}.md, features/F-001-{slug}.md, architecture.md (index ~50-80 lines) + architecture/*.md topic files (tech-stack, data-model, nfr, security, etc.). Optional: REVISIONS.md (after first --revise), prototypes/, .reviews/ (transient). Each leaf is self-contained (<300 lines, all referenced context copied inline). IDs: F-001, J-001, M-001 zero-padded sequential."
    status: confirmed
    rationale: "Directly derived from backup SKILL.md §Output Structure. Matches document-variant pyramid index pattern."
  R-004:
    # Input modality (conversational / file-ref / --interactive flag)
    value: "Multi-modal: (1) interactive `/prd-analysis` with no args (conversational questioning); (2) document-based `/prd-analysis path/to/notes.md` (parse provided file); (3) `--output docs/raw/prd/my-project` for custom output dir; (4) `--review <path>`, `--revise <path>`, `--evolve <path>` for existing-PRD operations. Primary FromScratch modes: interactive + document-based."
    status: confirmed
    rationale: "Explicit input modes enumerated in backup SKILL.md §Input Modes."
  R-005:
    # Structural review criteria (script-type CRs applicable to artifact type)
    value: "Script-type checks (deterministic, shell-enforceable): (a) artifact-pyramid shape — README.md index present, journeys/ and features/ directories non-empty, architecture.md index plus architecture/ topic files; (b) leaf size budget — each leaf file <300 lines; (c) ID format — F-NNN, J-NNN, M-NNN zero-padded sequential, no duplicates; (d) cross-reference integrity — journey Mapped Feature columns, feature Deps, feature Journey Context links, Cross-Journey Patterns 'Addressed by Feature' column all resolve; (e) required template sections present per prd-template / journey-template / feature-template / architecture-template; (f) file naming convention — kebab-case slugs; (g) artifact-nudity — no IPC HTML-comment envelopes leaked into leaves."
    status: confirmed
    rationale: "Derived from backup review-checklist.md structural dimensions and generative-skill guide §3.9 (artifact nudity) + §7.1 (pyramid shape)."
  R-006:
    # Semantic review criteria (LLM-type CRs)
    value: "LLM-type checks (semantic, intent-level): (a) self-contained principle — each feature/journey/module leaf reads independently, no orphan cross-references requiring external reads; (b) scope discipline — PRD captures product-level decisions only (what/for-whom/why/priority), does NOT drift into implementation (those belong to system-design); (c) journey-to-feature coverage — every feature maps back to at least one touchpoint; every cross-journey pattern addressed by at least one feature; (d) touchpoint completeness — stage, screen, action, interaction mode, system response, pain point all present; (e) design-token semantic naming (e.g. color.primary, spacing.md), no raw values; (f) acceptance criteria testable and measurable; (g) priority rationale present (P0/P1/P2); (h) persona consistency across journeys; (i) NFR coverage (performance, security, a11y, i18n, observability, deployment) when applicable to the product."
    status: confirmed
    rationale: "Derived from backup review-checklist.md (~50 dimensions across semantic/intent-level checks) and scope-reference.md PRD-vs-system-design boundary table."
  R-007:
    # New-version semantics (only relevant when evolving an existing skill)
    value: "not-applicable — FromScratch round-0, no prior skill version baseline to evolve from"
    status: not-applicable
    rationale: "Per orchestrator directive: FromScratch dispatch; no NewVersion baseline exists."

domain_terms_aligned:
  - term: "PRD"
    user_clarification: "Product Requirements Document — self-contained multi-file, optimized for AI coding agents"
    resolved_to: "artifact (pyramid: README.md index + journeys/ + features/ + architecture/ subdirectories under docs/raw/prd/)"
  - term: "self-contained multi-file"
    user_clarification: "each feature spec a standalone file"
    resolved_to: "leaf (single file at bottom of artifact pyramid, <300 lines, all context copied inline)"
  - term: "skill"
    user_clarification: "produces PRDs from sparse product ideas"
    resolved_to: "generative skill (per domain-glossary.md: produces artifacts from sparse input; 8-role pattern with iterative review/revise rounds)"
  - term: "AI coding agents"
    user_clarification: "downstream consumers of the PRD"
    resolved_to: "coding agents that read a single feature leaf to implement it (motivates self-contained-file principle)"

variant_replay:
  variant: "document"
  skeleton_path: "common/skeleton/document/"
  one_paragraph_summary: "Your skill will produce a multi-file markdown directory tree per the document-variant skeleton: a root README.md index (overview + journey/feature indices + roadmap), plus journeys/, features/, and architecture/ subdirectories containing self-contained leaf files. The skeleton provides the 8-role generative pattern (orchestrator + domain-consultant + planner + writers + cross/adversarial reviewers + reviser + summarizer + judge), mode routing (generate from-scratch | new-version | review | revise | --diagnose), and bootstrap-precheck + dispatch-log + pyramid-indexed .review/ directory. Script-type checks enforce structure (pyramid shape, leaf size <300 lines, ID format, cross-ref integrity); LLM-type checks enforce semantics (self-contained principle, PRD-vs-design scope discipline, journey-to-feature coverage, touchpoint completeness)."

notes: |
  Sparse-input path (word_count=29): HITL delegated to orchestrator per /proceed policy.
  All R-001..R-006 inferred from the @skills/prd-analysis.backup reference implementation
  expanded into input.md. R-007 = not-applicable for FromScratch. No requirements deferred —
  backup provides sufficient signal for all dimensions.
