---
issue_id: R3-001
round: 3
file: generate/domain-consultant-subagent.md
criterion_id: CR-L04
severity: error
source: cross-reviewer
reviewer_variant: cross
origin_trace: R3-V-001
origin_criterion: CR-L04
prev_rounds: [R2-001]
status: resolved
---

# R3-001 — domain-consultant subagent criteria numbering: RESOLVED

## Background

R2-001 filed a CR-L04 (criteria-internally-consistent) violation against
`generate/domain-consultant-subagent.md`: the file's R-005 and R-006 informational
listings plus the embedded `clarification.yml` template referenced pre-reclassification
criteria IDs (`PRD-S04` still listed as structural; `PRD-L01..L05` as 5 LLM criteria
with off-by-one names) that disagreed with the authoritative
`common/review-criteria.md` (7 structural: S01, S02, S03, S05, S06, S07, S08;
6 LLM: L01..L06 with L01 = feature-files-self-contained).

## Verification of fix (R3-R-001)

The round-3 snapshot of `generate/domain-consultant-subagent.md`
(sha256 `47f177a193abb9926b034045cb5c620c3097207caedf3ae3c842c04f4f15cc65`,
manifest line count 355) now shows:

1. **R-005 listing (lines 172–178)** — 7 entries, exactly matching the structural
   criteria IDs in `common/review-criteria.md`:

   - `PRD-S01` readme-frontmatter-complete
   - `PRD-S02` features-have-ids
   - `PRD-S03` journeys-have-ids
   - `PRD-S05` architecture-index-matches-topic-files
   - `PRD-S06` wikilink-targets-exist
   - `PRD-S07` revisions-log-consistency
   - `PRD-S08` leaf-size-within-limit

   Line 180 adds an explicit reclassification note pointing the reader to the R-006
   block for the former PRD-S04.

2. **R-006 listing (lines 188–193)** — 6 entries, exactly matching the semantic
   criteria IDs:

   - `PRD-L01` feature-files-self-contained (with parenthetical "reclassified from
     structural PRD-S04")
   - `PRD-L02` feature-to-journey-mapping
   - `PRD-L03` mvp-discipline
   - `PRD-L04` feature-boundaries-clear
   - `PRD-L05` non-functional-requirements-present
   - `PRD-L06` cross-journey-patterns-identified

3. **Output contract `clarification.yml` template (lines 306–326)** — R-005 and R-006
   blocks now emit the same 7+6 criteria IDs as above. The R-005 block's header
   text explicitly calls out the "PRD-S04 gap — reclassified as PRD-L01".

All three loci identified in R2-001 are now internally consistent with
`common/review-criteria.md`. Cross-checking the criterion-name pairs (ID ↔ name)
between the consultant file and the authoritative file yields no mismatches.

## Status transition

`new` (R2-001) → `resolved` (R3-001). The reviser (R2-R-001 per prompt; landed
before round 3) addressed every point called out in R2-001's Fix section.
