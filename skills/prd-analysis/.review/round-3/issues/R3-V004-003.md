---
id: R3-V004-003
round: 3
file: skills/prd-analysis/review/cross-reviewer-subagent.md
criterion_id: CR-L03
severity: error
source: cross-reviewer
reviewer_variant: cross
status: new
---

Section 9 ("LLM-Type Criteria from review-criteria.md") in the prd-analysis cross-reviewer
omits three LLM-type criteria that are defined in `skills/prd-analysis/common/review-criteria.md`
with `checker_type: llm`: CR-PP01, CR-PP04, and CR-PP05.

**Offending section (lines 289–338):** The enumerated list of LLM-type criteria begins at
CR-PP06 and ends at CR-PP51. Three LLM-type criteria are completely absent:

| Missing CR | Name | checker_type | severity |
|------------|------|-------------|---------|
| CR-PP01 | prd-directory-structure | llm | critical |
| CR-PP04 | no-tbd-remaining | llm | error |
| CR-PP05 | version-chain-integrity | llm | error |

**Evidence from `common/review-criteria.md`:**
- CR-PP01 YAML block (line 24–33): `checker_type: llm`, `severity: critical`
- CR-PP04 YAML block (line 83–93): `checker_type: llm`, `severity: error`
- CR-PP05 YAML block (line 102–114): `checker_type: llm`, `severity: error`

**Why it fails CR-L03 (review-criteria coverage):** CR-L03 requires that every CR id in
`common/review-criteria.md` must be referenced somewhere in the skill's sub-agent prompts.
These three LLM-type criteria are not referenced in Section 9 of `cross-reviewer-subagent.md`
and are therefore not evaluated during cross-review passes. This means:
- CR-PP01 (critical): the cross-reviewer will never flag a missing `README.md` or absent
  `journeys/`/`features/` directory structure in a PRD bundle.
- CR-PP04 (error): the cross-reviewer will never flag literal `TBD`, `TODO`, or `FIXME`
  markers remaining in any leaf.
- CR-PP05 (error): the cross-reviewer will never flag broken version chains in `REVISIONS.md`.

Note: CR-PP04 IS referenced in `writer-subagent.md` (line 211: "Any leaf | CR-PP04 (no TBD/TODO/FIXME)"),
so the writer self-reviews for it. But no cross-reviewer evaluation means self-review PARTIAL
rows for CR-PP04 will be unhandled.

**Class-based scan:** CR-PP01, CR-PP04, and CR-PP05 were also checked against `output-discipline.md`,
`parallel-dispatch.md`, `scope-reference.md`, and `common/review-criteria.md` — none of these
reference these CR IDs in a coverage context. The omission is isolated to this file's Section 9.
