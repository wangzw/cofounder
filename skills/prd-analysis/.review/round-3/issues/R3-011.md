---
id: R3-011
round: 3
file: review/adversarial-reviewer-subagent.md
criterion_id: CR-L01
severity: warning
source: cross-reviewer
reviewer_variant: cross
status: new
---

# Snippet D table still claims `clarification.yml` at round-N — cascade copy of R1-006 not fixed

## Offending text (verbatim)

Line 24 of `review/adversarial-reviewer-subagent.md` (Snippet D Role → final-path mapping
table):

> "| `domain_consultant` | 1 write | `.review/round-<N>/clarification.yml` |"

## Why this fails

R1-006 fixed the canonical owner (`generate/domain-consultant-subagent.md` line 33) to the
correct path `.review/round-0/clarification/<ISO-timestamp>.yml`. The adversarial reviewer's
Snippet D table still says `.review/round-<N>/clarification.yml` — a stale copy that
contradicts the canonical owner's authoritative write contract.

This violation is a class-based-scan finding from R1-006's incomplete cascade.

## Class-based scan results

Same class as R3-009 (writer-subagent.md), R3-010 (cross-reviewer-subagent.md),
R3-012 (per-issue-reviser-subagent.md). Four focus leaves still carry the stale row.

## Suggested fix

Edit `review/adversarial-reviewer-subagent.md` line 24:

```
| `domain_consultant` | 1 write | `.review/round-<N>/clarification.yml` |
```

→

```
| `domain_consultant` | 1 write | `.review/round-0/clarification/<ISO-timestamp>.yml` |
```
