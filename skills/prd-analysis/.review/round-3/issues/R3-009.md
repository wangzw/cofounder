---
id: R3-009
round: 3
file: generate/writer-subagent.md
criterion_id: CR-L01
severity: warning
source: cross-reviewer
reviewer_variant: cross
status: new
---

# Snippet D table still claims `clarification.yml` at round-N — cascade copy of R1-006 not fixed

## Offending text (verbatim)

Line 26 of `generate/writer-subagent.md` (Snippet D Role → final-path mapping table):

> "| `domain_consultant` | 1 write | `.review/round-<N>/clarification.yml` |"

## Why this fails

R1-006 fixed the canonical owner (`generate/domain-consultant-subagent.md` line 33) to the
correct path `.review/round-0/clarification/<ISO-timestamp>.yml`. The writer's own Snippet D
table still says `.review/round-<N>/clarification.yml` — a stale copy that contradicts:

1. The canonical owner's own write contract (`round-0/clarification/<ISO-timestamp>.yml`).
2. The writer's own Input Contract on line 134, which correctly reads
   `skills/prd-analysis/.review/round-0/clarification/<ts>.yml`.

Two inconsistencies in the same file: the writer's Snippet D table contradicts both the
domain-consultant's authoritative output contract and the writer's own input expectation. A
sub-agent template that disagrees with itself within 100 lines is exactly the documentation
defect CR-L01 (scope-discipline / authoritative-source-of-truth) is meant to catch.

This violation is a class-based-scan finding from R1-006 — the R1 author noted the cascade
explicitly: "I am NOT filing duplicate issues for those — they are mechanical copies of the same
Snippet D contents and the fix here cascades." The cascade was incomplete.

## Class-based scan results

Same class as R3-010 (cross-reviewer-subagent.md), R3-011 (adversarial-reviewer-subagent.md),
R3-012 (per-issue-reviser-subagent.md). Four focus leaves still carry the stale row.

## Suggested fix

Edit `generate/writer-subagent.md` line 26:

```
| `domain_consultant` | 1 write | `.review/round-<N>/clarification.yml` |
```

→

```
| `domain_consultant` | 1 write | `.review/round-0/clarification/<ISO-timestamp>.yml` |
```
