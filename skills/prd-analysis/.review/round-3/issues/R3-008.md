---
id: R3-008
round: 3
file: review/adversarial-reviewer-subagent.md
criterion_id: CR-L01
severity: error
source: cross-reviewer
reviewer_variant: cross
status: resolved
resolves: R1-008
---

# Verify R1-008 — Frontmatter key inconsistency: `issue_id:` (adversarial) vs `id:` (cross)

## R1 issue body (verbatim summary)

R1-008 flagged that the adversarial-reviewer's issue-file frontmatter schema (line 239) used the
key `issue_id:` while the cross-reviewer's schema (line 175) used `id:` — two different keys for
the same conceptual field. Downstream consumers (judge, summarizer, per-issue-reviser,
run-checkers.sh) match issues by frontmatter key, so the mismatch caused tooling that reads `id`
to silently skip adversarial-tier issues.

## Re-check result (round 3)

Read `review/adversarial-reviewer-subagent.md` lines 237-248 (current revision):

```yaml
---
id: R<N>-<seq>
round: <N>
file: <skill-root-relative-path-to-affected-leaf>
criterion_id: CR-L01
severity: critical | error | warning | info
source: adversarial-reviewer
reviewer_variant: adversarial
status: new | persistent | resolved | regressed
---
```

The frontmatter key is now `id:` (matching the cross-reviewer schema and the script-tier
issue emitter at `scripts/run-checkers.sh` line 530 which writes `f'id: {issue_id}\n'`). The
sequence placeholder is `<seq>` matching the cross-reviewer (R1-008's optional secondary fix
also applied).

Class-based grep for `issue_id:` (frontmatter key) across all 13 focus leaves returned zero
hits in any markdown frontmatter; the only `issue_id` occurrences in the focus set are
local Python variable names inside `scripts/run-checkers.sh` (lines 516, 518, 530), which is
correct script-internal usage.

## Status

`resolved` — same violation pattern is no longer detectable in the leaf.
