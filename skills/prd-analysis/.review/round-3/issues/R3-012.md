---
id: R3-012
round: 3
file: revise/per-issue-reviser-subagent.md
criterion_id: CR-L01
severity: warning
source: cross-reviewer
reviewer_variant: cross
status: new
---

# Snippet D table still claims `clarification.yml` at round-N — cascade copy of R1-006 not fixed

## Offending text (verbatim)

Line 34 of `revise/per-issue-reviser-subagent.md` (Snippet D Role → final-path mapping table):

> "| `domain_consultant` | 1 write | `.review/round-<N>/clarification.yml` (or scoped clarification path) |"

## Why this fails

R1-006 fixed the canonical owner (`generate/domain-consultant-subagent.md` line 33) to the
correct path `.review/round-0/clarification/<ISO-timestamp>.yml`. The per-issue-reviser's
Snippet D table still says `.review/round-<N>/clarification.yml (or scoped clarification path)`
— a stale copy that contradicts the canonical owner's authoritative write contract.

The misleading parenthetical "(or scoped clarification path)" was the original ambiguity R1-006
called out. It remains here even though R1-006 removed it from the domain-consultant.

This violation is a class-based-scan finding from R1-006's incomplete cascade.

## Class-based scan results

Same class as R3-009 (writer-subagent.md), R3-010 (cross-reviewer-subagent.md),
R3-011 (adversarial-reviewer-subagent.md). Four focus leaves still carry the stale row.

## Suggested fix

Edit `revise/per-issue-reviser-subagent.md` line 34:

```
| `domain_consultant` | 1 write | `.review/round-<N>/clarification.yml` (or scoped clarification path) |
```

→

```
| `domain_consultant` | 1 write | `.review/round-0/clarification/<ISO-timestamp>.yml` |
```
