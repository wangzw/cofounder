---
id: R3-010
round: 3
file: review/cross-reviewer-subagent.md
criterion_id: CR-L01
severity: warning
source: cross-reviewer
reviewer_variant: cross
status: new
---

# Snippet D table still claims `clarification.yml` at round-N — cascade copy of R1-006 not fixed

## Offending text (verbatim)

Line 35 of `review/cross-reviewer-subagent.md` (Snippet D Role → final-path mapping table):

> "| `domain_consultant` | 1 write | `.review/round-<N>/clarification.yml` (or scoped clarification path) |"

## Why this fails

R1-006 fixed the canonical owner (`generate/domain-consultant-subagent.md` line 33) to the
correct path `.review/round-0/clarification/<ISO-timestamp>.yml`. This cross-reviewer's Snippet
D table still says `.review/round-<N>/clarification.yml (or scoped clarification path)` — a
stale copy that contradicts the canonical owner's actual write contract.

The misleading parenthetical "(or scoped clarification path)" was the original ambiguity that
R1-006 called out as too vague to disambiguate the round-0 timestamped-subdirectory shape. It
remains in this leaf even though R1-006 removed it from the domain-consultant.

This violation is a class-based-scan finding from R1-006's incomplete cascade.

## Class-based scan results

Same class as R3-009 (writer-subagent.md), R3-011 (adversarial-reviewer-subagent.md),
R3-012 (per-issue-reviser-subagent.md). Four focus leaves still carry the stale row.

## Suggested fix

Edit `review/cross-reviewer-subagent.md` line 35:

```
| `domain_consultant` | 1 write | `.review/round-<N>/clarification.yml` (or scoped clarification path) |
```

→

```
| `domain_consultant` | 1 write | `.review/round-0/clarification/<ISO-timestamp>.yml` |
```
