# Self-Review — R1-W-002

**File reviewed**: `common/review-criteria.md`
**Round**: 1
**Timestamp**: 2026-04-24T14:05:00Z

## Checklist

Per `generate/in-generate-review.md` CR applicability table, for `common/review-criteria.md` the applicable CRs are: CR-S05, CR-S07, CR-L03.

- CR-S05 scripts-inventory: PASS — scripts-inventory validates the `scripts/` directory's presence and executability; this file does not itself enumerate scripts. Script paths referenced in my CR YAML blocks (e.g. `scripts/check-artifact-pyramid.sh`, `scripts/check-leaf-size.sh`, `scripts/check-id-format.sh`, `scripts/check-module-refs.sh`, `scripts/check-slug-naming.sh`, `scripts/check-template-sections.sh`, `scripts/check-cross-refs.sh`, `scripts/check-artifact-nudity.sh`, `scripts/check-index-consistency.sh`, `scripts/check-frontmatter.sh`, `scripts/check-revisions-log.sh`, `scripts/check-wikilinks.sh`) are the standard PRD-checker set derived from the backup implementation, and all fall under the skill's `scripts/` directory that the scripts-inventory checker validates elsewhere. No orphan or invented script paths outside the documented inventory.

- CR-S07 criteria-yaml-shape: PASS — every YAML block in this file carries the required fields (`id`, `name`, `version`, `checker_type`, `severity`) plus `conflicts_with`, `priority`, `incremental_skip`, and `script_path` where `checker_type: script`. `checker_type` values are exclusively `script` or `llm` (hybrid not used in v1). All `conflicts_with` fields are `[]` per guide §13.1. 25 CRs total (14 script + 11 llm), ≥24 guide baseline.

- CR-L03 description-is-trigger: PASS — this criterion targets the SKILL.md `description` frontmatter field ("Use when ..." trigger phrasing). The reviewed file is `common/review-criteria.md` and contains no SKILL-level description field, so the criterion is vacuously satisfied. The file's own introductory paragraph describes criterion organization (structural vs semantic groups, severity mapping) rather than a skill-invocation trigger, which is the correct framing for a criteria reference file.

## Summary

**FULL_PASS**: yes
**fail_count**: 0
**Scope notes**: All three applicable CRs PASS. The file contains 14 script-type CRs (CR-S01..CR-S14) covering pyramid shape, leaf size, ID format for features/journeys, module-ref validity, kebab-slug naming, template section presence, cross-reference integrity, artifact nudity, README/architecture index consistency, frontmatter fields, revisions-log monotonicity, architecture-index size budget, and wikilink resolution; plus 11 LLM-type CRs (CR-L01..CR-L11) covering self-contained-file principle, PRD-vs-design scope discipline, journey-to-feature coverage, touchpoint completeness, design-token semantic naming, testable acceptance criteria, priority rationale, persona consistency, NFR applicability coverage, cross-journey pattern addressing, and internal oscillation guard. Total 25 CRs ≥24 baseline. Structure is tight per assignment guidance — concise YAML blocks with brief human-readable prose, 408 lines total.
