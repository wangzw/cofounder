# Self-Review — R1-W-004

**File reviewed**: `common/templates/artifact-template.md`
**Round**: 1
**Timestamp**: 2026-04-24T14:05:00Z

## Checklist

Per `generate/in-generate-review.md` CR Applicability Table — Template files
(`common/templates/*.md`) row: applies CR-L02, CR-L07 (severity floor: error).

- CR-L02 self-contained-file: PASS — The template explicitly encodes the self-contained
  inline-copy principle as a universal rule applying to ALL leaves, with concrete examples
  (feature Context section copies architecture fragments, data models, conventions, and
  journey touchpoint details inline; cross-references "see architecture.md" / "see J-003"
  are FORBIDDEN). Hyperlinks are permitted for navigation only and explicitly marked as
  non-load-bearing. Topic-leaf rules reiterate "no 'see other-topic.md' cross-links that
  carry load-bearing content". The template itself contains no load-bearing external
  reference — the one reference to the backup's architecture-template.md is bounded to
  inheriting a fixed, enumerated topic-file list (the list is itself fully copied inline
  in the section), so the template remains self-contained.
- CR-L07 reviewer-prompt-discipline: PASS — All normative clauses in the template use
  MUST, MUST NOT, FORBIDDEN, SHOULD. Soft language ("try to", "prefer", "ideally") is
  absent from hard-check statements. Examples: "every leaf MUST be ≤ 300 lines", "Raw
  values (...) are FORBIDDEN", "Empty sections are FORBIDDEN", "Cross-references of the
  form 'see architecture.md for tokens' — MUST copy the specific tokens this feature uses
  inline". The one "SHOULD" usage ("Every journey pain point SHOULD have feature coverage")
  is intentional because uncovered pain points are flagged as warning-level (not blocker)
  per the Cross-Leaf Consistency section, matching the severity of the underlying check.

## Summary

**FULL_PASS**: yes
**fail_count**: 0
**Scope notes**: Both applicable CRs pass. Template is 319 lines — above the 300-line PRD
leaf budget but within the reasonable range for a reference catalog that describes five
distinct leaf types (README, journey, feature, architecture index, architecture topic).
The 300-line budget is a constraint on generated PRD leaves, not on meta-template files;
no CR in the applicable set enforces template-file line count. No blockers identified;
no scope-external conflicts with other round-1 leaves detected in the writer scope (the
template only constrains downstream writers authoring PRDs — it does not depend on other
skill-internal leaves being produced in this round).
