---
id: R1-003
round: 1
file: generate/writer-subagent.md
criterion_id: CR-L01
severity: error
source: cross-reviewer
reviewer_variant: cross
status: new
---

# Stale CR-ID format in writer-subagent.md (5 instances)

## Offending text (verbatim)

Line 171 of `generate/writer-subagent.md`:

> "Leaf size cap: ≤300 lines (CR-S-002). If the artifact body would exceed 300 lines, split into narrower leaves and report the split in the self-review."

Line 286:

> "Violating this principle makes the PRD unusable by automated coding agents (CR-L-004)."

Line 424:

> "### BAD — Self-Contained Rule Violated (CR-L-004 fires)"

Line 435:

> "**Why this fails**: A coding agent reading F-003 MUST open 3 additional files to understand the feature. This breaks the self-contained file principle (CR-L-004). All three blocks of context MUST be copied inline as shown in the GOOD example above."

Line 440:

> "### BAD — Untestable Acceptance Criterion (CR-L-006 fires)"

Line 451:

> "...it impossible to verify implementation — CR-L-006 fires."

Line 455:

> "### BAD — MVP Discipline Violated (CR-L-005 fires)"

Line 469:

> "...biometric) inside a P0 feature leaf. This violates MVP discipline (CR-L-005)."

## Why this fails

`common/review-criteria.md` defines criteria using the canonical IDs `CR-S02` (leaf-size-cap), `CR-L04` (self-contained-readability), `CR-L05` (mvp-discipline), `CR-L06` (ambiguity-elimination). The dashed forms `CR-S-002`, `CR-L-004`, `CR-L-005`, `CR-L-006` used in this writer prompt are not defined IDs. Writers reading this prompt will copy the dashed format into their own self-reviews and into BAD-example annotations, propagating the stale-ID class through every PRD that the skill produces. Per the cross-reviewer spec: "FORBIDDEN to reference CR IDs not defined in `common/review-criteria.md`".

## Class-based scan results

Same dashed-format class as R1-001, R1-002. Eight separate occurrences in this file (lines 171, 286, 424, 435, 440, 451, 455, 469). One issue per affected leaf per cross-reviewer spec.

## Suggested fix

Use `MultiEdit` to replace all 8 occurrences with their canonical equivalents:

| Stale | Canonical |
|-------|-----------|
| `CR-S-002` | `CR-S02` |
| `CR-L-004` | `CR-L04` |
| `CR-L-005` | `CR-L05` |
| `CR-L-006` | `CR-L06` |
