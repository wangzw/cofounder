---
id: R1-002
round: 1
file: common/domain-glossary.md
criterion_id: CR-L01
severity: error
source: cross-reviewer
reviewer_variant: cross
status: new
---

# Stale CR-ID format in domain-glossary.md (`CR-L-004`)

## Offending text (verbatim)

Lines 277-279 of `common/domain-glossary.md` (Self-Contained Leaf disambiguation prose):

> "...The self-contained principle is frequently violated by writers who add cross-references like 'see architecture.md for data models' or 'refer to journey J-001 for context'. In this skill, every leaf MUST copy the context it needs inline. A file that points to another file for essential context is NOT self-contained and fails CR-L-004."

## Why this fails

`common/review-criteria.md` defines the self-containment criterion as `CR-L04 self-contained-readability`, not `CR-L-004`. The dashed form is not a defined criterion ID. Downstream agents that match CR-IDs verbatim (cross-reviewer, run-checkers.sh) will not link a "fails CR-L-004" reference back to the real `CR-L04` criterion definition — the cross-reference is broken.

## Class-based scan results

Same dashed-format class as R1-001 (SKILL.md), R1-003 (writer-subagent.md). One occurrence in this file (line 278). Issue is filed per-file per cross-reviewer spec ("Each issue file covers ONE leaf").

## Suggested fix

Replace `CR-L-004` with `CR-L04` on line 278.
