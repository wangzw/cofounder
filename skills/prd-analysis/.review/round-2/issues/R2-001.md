---
id: R2-001
round: 2
file: "common/review-criteria.md"
criterion_id: CR-META-missing-checker
severity: error
source: cross-reviewer
reviewer_variant: cross
status: resolved
resolved_script_path: "scripts/check-leaf-size.sh"
resolves: R1-001
---

# CR-META-missing-checker — resolved

R1-001 flagged CR-S02 as declaring `script_path: scripts/check-leaf-size.sh` with no such script on disk. Round-1 reviser converted CR-S02 in `common/review-criteria.md` to `checker_type: llm` and moved the path to `script_pending: "scripts/check-leaf-size.sh"`, so the CR now runs as an LLM check and `run-checkers.sh` no longer emits a CR-META-missing-checker issue for it.

Current state (`common/review-criteria.md` lines 36–45):

```yaml
- id: CR-S02
  name: "leaf-size-budget-300"
  version: 1.0.0
  checker_type: llm
  script_pending: "scripts/check-leaf-size.sh"
  severity: error
  conflicts_with: []
  priority: 2
  incremental_skip: per_file
```

Round-2 run-checkers output is `[]` — confirming the issue is no longer detectable. The `resolved_script_path` field preserves traceability to the originally-missing script.
