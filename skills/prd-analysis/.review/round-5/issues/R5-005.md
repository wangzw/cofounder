---
id: R5-005
round: 5
file: shared/summarizer-subagent.md
criterion_id: CR-L01
severity: warning
source: cross-reviewer
reviewer_variant: cross
status: new
---

# Snippet D row for `domain_consultant` still claims `round-<N>/clarification.yml` — same R1-006 cascade class, summarizer leaf was missed

## Offending text (verbatim)

Line 34 of `shared/summarizer-subagent.md` (Snippet D Role → final-path mapping table):

> "| `domain_consultant` | 1 write | `.review/round-<N>/clarification.yml` (or scoped clarification path) |"

## Why this fails

Same defect class as R3-009..012. The R1-006 cascade fixed the canonical owner
(`generate/domain-consultant-subagent.md` line 33) to
`.review/round-0/clarification/<ISO-timestamp>.yml`. R3-009..012 closed the cascade for
the writer, both reviewers, and the per-issue reviser. The summarizer leaf — also a
Snippet-D-bearing sub-agent template — still carries the stale row, including the
ambiguous "(or scoped clarification path)" parenthetical that R1-006 explicitly removed
from the canonical owner.

`shared/summarizer-subagent.md` is in `cross_reviewer_focus` (CR-S08) per
`.review/round-5/skip-set.yml` line 69, so this leaf is in scope for cross-review.

A class-based scan across all 13 cross-reviewer focus leaves (`grep -rn
"round-<N>/clarification\|round-N/clarification"`) found exactly one remaining match —
this one. All other Snippet-D-bearing leaves are clean.

## Class-based scan results (post-revise)

| File | Snippet D `domain_consultant` row |
|------|------------------------------------|
| `generate/domain-consultant-subagent.md:33` | OK — `round-0/clarification/<ISO-timestamp>.yml` |
| `generate/writer-subagent.md:26` | OK — round-0 path (R3-009 fixed) |
| `review/cross-reviewer-subagent.md:35` | OK — round-0 path (R3-010 fixed) |
| `review/adversarial-reviewer-subagent.md:24` | OK — round-0 path (R3-011 fixed) |
| `revise/per-issue-reviser-subagent.md:34` | OK — round-0 path (R3-012 fixed) |
| `shared/summarizer-subagent.md:34` | STALE — still `round-<N>/clarification.yml (or scoped clarification path)` |

## Suggested fix

Edit `shared/summarizer-subagent.md` line 34:

```
| `domain_consultant` | 1 write | `.review/round-<N>/clarification.yml` (or scoped clarification path) |
```

→

```
| `domain_consultant` | 1 write | `.review/round-0/clarification/<ISO-timestamp>.yml` |
```

After this edit the R1-006 cascade is fully closed across all six Snippet-D-bearing
sub-agent templates.
