---
id: R7-V001-003
round: 7
file: common/parallel-dispatch.md
criterion_id: CR-L11
severity: error
source: cross-reviewer
reviewer_variant: cross
status: new
---

# R7-V001-003: common/parallel-dispatch.md cross-references `revise/revise-mode.md` Step 5 — perpetuates the dual-spec drift addressed by R7-V001-002

## Finding

`common/parallel-dispatch.md` line 134:

```
See `review/index.md` Step 2 and `revise/revise-mode.md` Step 5 for the full templates that bake
```

(referencing the Fix-subagent dispatch templates).

This locks `revise/revise-mode.md` into the canonical scaffolding (parallel-dispatch is loaded by every mode per SKILL.md mode-routing). Per R7-V001-002, `revise/revise-mode.md` is the legacy interactive document, while `revise/index.md` is the canonical generative-skill orchestration. Two files cannot both be the authoritative source for "Step 5 dispatch templates" for the same `--revise` mode.

## Class-based scan note

This is the same class as R7-V001-002 (dual-spec drift with `revise/revise-mode.md` as the canonical reference). Per cross-reviewer-subagent.md "Class-Based Scan (MANDATORY)", I scanned all focus leaves for the same pattern; this is the second instance, filed separately per the "one issue file per leaf" rule.

## CR-L11 reasoning

Same as R7-V001-002 — a stated contract in `parallel-dispatch.md` references an implementation file (`revise-mode.md`) whose role conflicts with `revise/index.md`. CR-L11 cross-reference-consistency violation.

## Suggested fix

When R7-V001-002 is resolved (folding `revise-mode.md` content into `revise/index.md` or deleting it):
- Update `common/parallel-dispatch.md` line 134 to point at the surviving file (likely `revise/index.md` Step 2 or a new sub-section under it).
- If both `review-mode` and `revise-mode` end up consolidated into their `index.md` siblings, update both halves of the line consistently.

This issue is satellite to R7-V001-002 — should be batched with it in the same revise-mode-cleanup revise dispatch.
