---
id: R6-003
round: 6
file: review/cross-reviewer-subagent.md
criterion_id: CR-L11
severity: error
source: cross-reviewer
reviewer_variant: cross
status: resolved
resolves: R4-003
---

# CR-L11 criteria-internally-consistent — resolved

Verified against revised `review/cross-reviewer-subagent.md`.

Evidence of fix:

- Line 148: now reads "Authoritative definitions for CR-L01..CR-L11 plus PRD-domain LLM-type CRs (...including criteria-internally-consistent)". Previously stopped at CR-L10.
- The stale "CR-L07 language discipline" references on R4-era lines 229, 319, 389 are removed. The current house-style guidance (lines 238–243 and lines 331–334) explicitly labels the soft-language rule as "a house style rule for reviewer-written issue prose (no corresponding CR in `common/review-criteria.md`)" and adds "the meta-review pass flags reviewer-authored soft language directly rather than via a criterion citation." The Anti-pattern A annotation (lines 404–405) now reads "soft language in a hard check (house style rule; no CR cite — the meta-review pass flags soft language directly)" rather than "(CR-L07 fires)".
- Line 225–227 adds a FORBIDDEN rule: "FORBIDDEN to invent CR IDs — if no canonical CR matches the intent, either use `CR-META-*` for skip-set/regression/skeleton concerns or omit the citation (do not fabricate `CR-PRD-*` or other ad-hoc prefixes)."
- Positive Example frontmatter (lines 376–385): `criterion_id: CR-L03` (canonical journey-to-feature-coverage). Previously `CR-PRD-journey-feature-coverage`.

Every CR-ID referenced in cross-reviewer-subagent.md now resolves to a canonical definition in `common/review-criteria.md` (or uses `CR-META-*` for non-CR concerns). The R4-003 violation is no longer detectable.
