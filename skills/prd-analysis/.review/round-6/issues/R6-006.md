---
id: R6-006
round: 6
file: review/cross-reviewer-subagent.md
criterion_id: CR-L11
severity: warning
source: cross-reviewer
reviewer_variant: cross
status: resolved
resolves: R4-006
---

# CR-L11 criteria-internally-consistent — resolved

Verified against revised `review/cross-reviewer-subagent.md` §Output Contract — Issue Files (lines 191–217).

Evidence of fix:

- Line 196–197: "Issue ID format: `R<N>-<seq>` where `<N>` is the round number and `<seq>` is zero-padded 3 digits (e.g. `R3-007`). This matches the on-disk convention used by round-1/round-2 issue files."
- Lines 197–202: explicitly documents that the format is shared across the three write-to-issues roles — "shared with the adversarial-reviewer and per-issue-reviser sub-agents — all three write to the same `<target>/.review/round-<N>/issues/` directory, so the frontmatter schema MUST be identical across roles."
- Lines 206–217: canonical frontmatter schema now uses `id: R<N>-<seq>` (not `issue_id: prd-analysis-round-<N>-<seq>`). The schema includes all the fields the reviser and aggregate.py parsers consume (round, file, criterion_id, severity, source, reviewer_variant, status).
- Positive Example (lines 376–385) demonstrates the unified format: `id: R2-007`.

The cross-reviewer-subagent.md's own issue_id schema is now aligned with the `R<N>-<seq>` convention used by round-1/round-2 on-disk files. R4-006's concern on cross-reviewer-subagent.md is resolved.

Note: sibling prompts (adversarial-reviewer-subagent.md line 324 `issue_id: <target-slug>-round-<N>-<seq>`; per-issue-reviser-subagent.md line 230 `issue_id: <prd-slug>-round-<N>-<next-seq>`) remain unchanged in the round-5 reviser dispatch and diverge from the cross-reviewer's canonical schema. This residual divergence is filed as a new finding at `R6-007` (not a regression of R4-006, whose file-scope was only cross-reviewer-subagent.md).
