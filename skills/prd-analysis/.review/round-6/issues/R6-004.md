---
id: R6-004
round: 6
file: generate/planner-subagent.md
criterion_id: CR-L01
severity: error
source: cross-reviewer
reviewer_variant: cross
status: resolved
resolves: R4-004
---

# CR-L01 self-contained-file — resolved

Verified against revised `generate/planner-subagent.md`.

Evidence of fix:

- Lines 78–90 now route all five leaf kinds (README, journey, feature, architecture-index, architecture-topic) through the single canonical template `common/templates/artifact-template.md` with an added `template_section` field (e.g. `"§ README Template"`, `"§ Journey Template"`, `"§ Feature Template"`, `"§ Architecture Index Template"`, `"§ Architecture Topic Template"`).
- Lines 135–154 (YAML block shape) show every `add` entry using `template: "common/templates/artifact-template.md"` + `template_section` — no occurrence of the four non-existent template paths (`prd-template.md`, `journey-template.md`, `feature-template.md`, `architecture-template.md`).
- Line 111–112 (Tombstone handling) now specifies `template: common/templates/artifact-template.md` and `template_section: "§ Tombstone Template"`.
- Line 167: "`template`: ... This MUST be `common/templates/artifact-template.md` — the single canonical template for every PRD leaf kind."
- Line 169–174: `template_section` field is explicitly documented as required whenever `template` is non-null.

The four non-existent template paths cited in R4-004 (`prd-template.md`, `journey-template.md`, `feature-template.md`, `architecture-template.md`) no longer appear anywhere in planner-subagent.md. Every `add`/`modify` entry now references a file that actually exists on disk (`common/templates/artifact-template.md`), satisfying the self-contained-file constraint for downstream writer dispatches.

Cross-ref check: writer-subagent.md Input Contract (line 104) reads `common/templates/artifact-template.md` with section selection via `leaf_kind` / `template_section` — planner and writer are now aligned.

The R4-004 violation is no longer detectable.
