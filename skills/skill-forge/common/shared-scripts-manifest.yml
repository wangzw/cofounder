# NOTE: lib/aggregate.py has drifted from upstream by two fixes:
#   1. Support for guide §3.8 two-event dispatch-log format
#      (launched+completed merged by trace_id).
#   2. TRACE_ID_RE tightened from `[A-Za-z]-\d+` to canonical
#      `[CPWVRSJ]-\d{3}` per CR-S10 / R6-V003-004 (round 6, delivery 3).
# The upstream attachment expects only combined-format records and uses the
# permissive regex. See commits that introduced these drifts for details.
# A future upstream sync should reconcile.
# sha256 pins for verbatim-copied scripts from attachments/metrics-aggregate/
# Check enforced by scripts/check-scaffold-sha.sh (CR-S12)
files:
  scripts/metrics-aggregate.sh:
    sha256: 70d2c3dad1d974ea2804002959465b29e1ca513d412acc59a04e5c812fc2a19f
    snapshot_date: 2026-04-24
  scripts/lib/aggregate.py:
    sha256: 659f6abe2158e0f02fe41d5b2bee87e848bae6b5115df80d2dd21f4a63744979
    snapshot_date: 2026-04-24
