# Artifact Template — {{SKILL_NAME}} (code variant)

<!-- DOMAIN_FILL: populated by writer-subagent during round 1 -->

This file defines the canonical structure for code artifacts produced by {{SKILL_NAME}}. The writer sub-agent uses this template as a structural scaffold when authoring new code files.

## Artifact Structure (code variant)

Code artifacts should specify:
- Target language and version (e.g., Python 3.11, TypeScript 5.x)
- Module/package structure if multi-file
- Entry point and exported symbols
- Dependency declarations (imports, package.json, requirements.txt)

<!-- Writer: describe the code artifact's file layout, language conventions, and module structure -->

## Required Elements

<!-- Writer: list mandatory code elements (e.g., type annotations, docstrings, error handling) -->

## Optional Elements

<!-- Writer: list optional elements (e.g., tests, examples, CLI entry points) -->

## Style Constraints

<!-- Writer: specify style rules (e.g., line length, naming conventions, formatter config) -->

## Example

<!-- Writer: provide a minimal valid code artifact conforming to this template -->
